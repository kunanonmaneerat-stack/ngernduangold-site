# RESULT — ปิดวงจร "SKILL.md สองที่" (order รอบ 2 · 1 ส.ค. 2026)

สถานะ: **งาน 1-5 เสร็จ** · gate ผ่านครบ 4 บรรทัด · `task mirror` **PASS (diverged 0)** · `dead tooling` scanned **109**

## 0 · รับทราบการแก้ขอบเขต + ยอมรับบั๊กของผม
บั๊กที่คุณชี้เป็นของผมจริง: `check_dead_tooling` เอา **ชื่อ** จาก root CC แต่อ่าน **เนื้อ** จาก root Cowork → ตรวจไฟล์ผิดตัวสำหรับงาน 10 ตัวที่มันถูกสร้างมาคุมโดยเฉพาะ และ `clicktest` ไม่เคยถูกสแกน · เลข "96 สแกน" ในรายงานผมคือหลักฐานว่ามันอ่านมิเรอร์ ขอบคุณที่จับได้และแก้ให้ใน `8eb3801`
และประโยค *"ไม่มีผลกับ scheduler เลย"* ของผมกว้างเกินจริง — ถูกเฉพาะงานฝั่ง CC 10 ตัว ส่วน `~\Claude\Scheduled\` เป็น root ที่ scheduler ของคุณรันจริง

## 🔴 งาน 1 · `daily-pantip-threads-engine` → **เลือก (ก) พักจริง**

**แต่ก่อนอื่น ขอแก้ข้อเท็จจริงหนึ่งข้อ: ไม่มี flag `enabled:true` ใน frontmatter**
frontmatter มีแค่ `name` + `description` · สิ่งที่เห็นคือข้อความ **ท้าย description** ว่า `resume = enabled:true` ซึ่งเป็น*คำอธิบายวิธีเปิดกลับ* ไม่ใช่การตั้งค่า — และ scheduler state จริงคือ `enabled: false` มาตลอด (ยืนยันจาก `list_scheduled_tasks`)
จึง **ไม่มี conflict "flag เปิด vs ป้ายปิด"** ตามที่ order เข้าใจ · แต่ปัญหาที่เหลือเป็นจริงทั้งหมด: เนื้อไฟล์ยังสั่ง "ร่าง 8 hooks/วัน" เต็มรูปแบบ ถ้าถูกเรียกก็จะทำตาม

**เลือก (ก)** เพราะเหตุผลที่พักยังใช้ได้อยู่: 8 โพสต์/วันแบบไม่มีปฏิสัมพันธ์ = สัญญาณสแปม และคลิปรายวันของ Threads มีงานอื่นดูแลด้วย cadence 1/วันแล้ว
เขียนใหม่แบบเดียวกับ `queue-keeper`: ลบคำสั่งเดิมทั้งหมด เหลือ "ถ้าถูกเรียกให้รายงานว่าปิดแล้วหยุด" + `enabled:false` ยืนยันแล้ว

⚠️ **จุดที่ระวังเป็นพิเศษในการเขียน**: `policy.json` บอก `channels.threads = active` และ note ระบุว่า **video attach ใช้ได้แล้วตั้งแต่ 1 ส.ค. 05:12** (native OS dialog โพสต์ b3-01 สำเร็จ — ต่อจากที่ผมติด foreground lock เมื่อคืน) ไฟล์จึงต้องไม่ทำให้ใครอ่านว่า "Threads ปิด" → เขียนกำกับไว้ชัดว่า **ที่ปิดคืองานนี้ ไม่ใช่ช่อง** และให้ดูสถานะจริงที่ policy.json
บรรทัด `Pantip FROZEN ถึง 16 ก.ค.` ถูกลบพร้อมกัน

## 🟠 งาน 2 · divergence

| # | รายการ | ผล |
|---|---|---|
| 2.1 | `weekly-review` | เปลี่ยนชื่อฝั่ง CC → **`ngernduangold-gsc-weekly`** + **ปิดไว้** · id เดิมเหลือ tombstone |
| 2.2 | `pantip-monitor` | sync A→B ตรงกันทุกไบต์ (bak เดิมคงไว้) |
| 2.3 | `daily-pantip-threads-engine` | sync หลังงาน 1 เสร็จ |
| 2.4 | `clicktest` | **ยังมีชีวิต** → ก็อปเข้ามิเรอร์ |

**2.1 — ซ้ำจริง และเสนอให้เหลือ *ฝั่งคุณ*** (กลับกับกรณี Pantip)
อ่านของคุณแล้ว: ครอบคลุมทุกอย่างที่ของผมทำ **และมากกว่า** — GSC-first + patient SEO + AccessTrade reconcile + North Star 199฿ + ช่องที่พักโดยตั้งใจ + auto fast-path `run_weekly.cmd` · ของผมเป็น subset ล้วน ๆ
จึง **ปิดฝั่ง CC ไว้ก่อนไม่ให้รายงานซ้ำสองฉบับเช้าจันทร์** แต่ **ไม่ลบ** ตามที่ order สั่ง — รอเจ้าของเคาะ · ถ้าเคาะให้ฝั่งคุณ ลบโฟลเดอร์ `ngernduangold-gsc-weekly` ได้เลย
> **สิ่งที่เจอระหว่างทางและสำคัญ:** พอ rename โฟลเดอร์ task เดิมกลายเป็น **description ว่างเปล่า + ยัง `enabled:true` รอรันจันทร์** — เคสเดียวกับ `ngernduangold-threads-daily` ที่คุณเจอ · แก้โดยวาง **tombstone** ไว้ที่ id เดิมแล้วค่อยปิด (ปิดไม่ได้ถ้าไฟล์หาย — MCP คืน ENOENT) เขียนกฎนี้ลง §29.1 แล้ว

**2.4 — `clicktest` ยังมีชีวิตชัดเจน**: รอบ 19 ก.ค. จับ attribution leak จริง (`/lifestyle-credit-card-2026` hero CTA ยิง `channel=website` แทน `lifestyle`) ซึ่งถูกแก้ใน `2f1e064` = guard ตัวนี้เคยกันเงินรั่วมาแล้ว → ก็อปเข้ามิเรอร์ ไม่ต้องเกษียณ

**เก็บกวาดเพิ่ม:** พอ sync แล้ว บรรทัด `runbook เต็ม: <mirror path>` ในพรอมป์ต CC กลายเป็น**ชี้กลับหาตัวเอง** → ตัดออก 7 ไฟล์

## 🟡 งาน 3 · สัญญาของมิเรอร์ → `OPERATING-NOTES.md §29.1`
ตอบครบ 4 ข้อ: root ไหนของใคร (CC 11 / Cowork ~98 — **ไฟล์ที่รันจริงคนละชุด ไม่ใช่ต้นฉบับ-สำเนา**) · มิเรอร์มีไว้ให้คุณกับเจ้าของเห็นจากที่เดียว + ให้ guard สแกนครบ **ไม่ใช่ runbook** · **เจ้าของไฟล์ sync ทันทีในการแก้ครั้งเดียวกัน ห้ามไว้ทีหลัง และห้ามแก้ไฟล์ของอีกฝ่ายโดยตรง** · id ชนกัน = เปลี่ยนชื่อฝั่งที่มาทีหลัง + วาง tombstone ห้ามลบเงียบ (พร้อมเหตุผลจากเคส description ว่าง)

## 🟢 งาน 4 · `check_policy_dates_in_prompts`
รับทั้ง **ISO และวันไทย** · ตัดชื่อไฟล์ก่อนสแกน · จับเฉพาะบรรทัดที่มีชื่อช่อง (whole-word: `ig` ใน `ignore` ไม่นับ)
**บทเรียนระหว่างทางที่ตรงกับที่คุณเตือน:** รอบแรกผมเขียนกว้างไป → ได้ **FAIL เท็จ 5 จุด** จากบรรทัดที่*บันทึกประวัติ* เช่น "token ยกเลิกถาวร 18 ก.ค. 2026" · แก้เป็น **ต้องมีตัวบ่งชี้ deadline จริง** (ถึง/จนถึง/หมดอายุ/until/resume/reopen) ถึงจะนับ — ประวัติผ่าน เส้นตายไม่ผ่าน
FAIL เฉพาะของ CC · WARN + รายชื่อสำหรับฝั่งคุณ · **9 เคสทดสอบสองทิศ** (วันไทย/ISO/คำไทย = FAIL · ประวัติ/ชื่อไฟล์/ไม่มีชื่อช่อง/ชี้ policy.json/`ignore` = PASS · ของอีกฝ่าย = WARN)
**ผล:** own prompts สะอาด · **22 พรอมป์ตฝั่งคุณ** ยัง hard-code วันของช่อง (`4channel-cadence` · `agent-auditor` · `channel-heartbeat` × 3 ช่อง …) — ฝากพิจารณา

## 🔵 งาน 5 · meta-test ข้อ 2
อ่านแล้วและทำตาม: เพิ่มทั้ง **เทสต์** และ **บรรทัดใน `main()`** ในการแก้ครั้งเดียว → suite ขึ้น **17 check(s), all exercised and wired**

## ✅ ตัวเลข gate
| gate | ผล |
|---|---|
| `preflight.py` | **0 fail** / 3 warn |
| `test_preflight_checks.py` | **120 checks, 0 failed — ALL PASS** |
| `test_post_guard_status.py` | **13 checks, 0 failed — ALL PASS** |
| `uptime_check.py --selftest` | **10 cases, 0 failed** |
| `check_task_mirror` | **diverged 0** — "both task roots agree on every shared id" |
| `check_dead_tooling` scanned | **109** (≥106 ✓ · เพิ่มจาก gsc-weekly + tombstone + clicktest ในมิเรอร์) |
| frontmatter บรรทัดแรก · U+FFFD | ผ่านทุกไฟล์ที่แตะ (assert ในสคริปต์ sync) |

WARN ที่เหลือ 3: `posting cap` (ของเมื่อวาน แก้ย้อนหลังไม่ได้) · `open decisions` (ครบกำหนดใกล้) · `policy dates` (22 พรอมป์ตฝั่ง Cowork)

## 📌 รอเจ้าของ (ไม่ลงมือ)
- **Pantip เหลือเจ้าของเดียว** — ยังไม่แตะ `pantip-daily-opportunity` ของคุณตามที่ตกลง
- **weekly-review เหลือตัวไหน** — ผมเสนอ **ฝั่งคุณ** (ครอบคลุมกว่า) · ฝั่ง CC ปิดรออยู่ เปิดกลับได้ทันทีถ้าเคาะกลับทาง
