# RESULT — CC-ORDER master batch 23 ก.ค. 2026 (A→D)

ทำโดย: CC · 23 ก.ค. 23:29 · สถานะ: **A/B/C/D เสร็จครบ** · ปัญหาสำคัญที่พบ: **order-factfix-20260720 (P0) ยังไม่ถูกทำ — ห้ามย้าย จึงรายงานแทน** (รายละเอียดท้ายไฟล์)

## A. internal-link boost — ✅ เสร็จ + gate PASS

**แก้ 7 source pages · เพิ่ม 9 ลิงก์** (dup-check ตามกติกา order ทำให้ข้าม 1 หน้า):

| target | ก่อน (referring) | หลัง | ลิงก์ใหม่จาก |
|---|---|---|---|
| C debt-letter-kit (orphan-fix) | 1 | **5** ✅ (เกณฑ์ ≥3) | debt-restructuring (§เจรจากับแบงก์) · pay-off-credit-card (§ลดดอกระหว่างทาง) · debt-clinic-sam (§ถ้าไม่เข้าเกณฑ์) · debt-consolidation (§คืออะไร) |
| A car-still-installment-loan-2026 | 2 | **5** | loan-cash (§จำนำทะเบียนรถ) · debt-restructuring (li รีไฟแนนซ์/รวมหนี้) · debt-consolidation (§คุ้มเมื่อไหร่) |
| B credit-card-salary-30000-2026 | 5 | **7** | salary-15000 (§สมัครได้จริงไหม) · credit-card-documents (ul เอกสารพนักงานประจำ) |

- **ข้าม salary-20000 → B**: หน้านั้นมีลิงก์เข้า target อยู่แล้วใน build ปัจจุบัน — กติกา order ข้อ dup-check ("มีอยู่แล้ว = ข้าม ไม่ซ้ำ") จึงเหลือ B 2 source (decision บันทึกไว้ตรงนี้)
- หมายเหตุ C: ลิงก์เดิมจาก letter_cta เป็น CTA banner + `?utm` (ไม่ใช่ contextual และ grep ลิงก์สะอาดไม่นับ) — 4 ลิงก์ใหม่เป็น `.ilinks` ในเนื้อความ href สะอาด extensionless ตาม order
- anchor ตามที่ order บังคับทุกตัว ไม่มีคำต้องห้าม/%/ราคา · 1 ลิงก์ต่อ source ต่อ target
- **BUILD GATE PASS**: build ผ่าน · `postdeploy_smoke --src site` **71/71 หน้า · ปุ่ม atth.me 197** · affiliate 19/19 (ไม่เพิ่ม/หาย) · link_check broken 0
- วิธีแก้: python I/O anchored-replace (byte-safe) เฉพาะ build_site.py · verify นับลิงก์ใน site/*.html ก่อน/หลังตามตาราง
- หมายเหตุ: `git add build_site.py site/` ตาม order เก่าทำไม่ได้ตรงตัว — **site/ เป็น build-output ที่ gitignore** (Netlify build เอง) จึง add เฉพาะ build_site.py

## B. push ทั้งหมด — ✅

- `ae8a0a8` (quote-card layer) **อยู่บน remote แล้วก่อนเริ่มงาน** · ที่ค้างจริงคือ `ec9f63f` (ledger backfill) + `8c9893a` (master order) → ขึ้นไปกับ push รอบนี้
- commit ใหม่รอบนี้ (เรียงตาม push): state-sync runlog/manifest → C-fix (tools/social-autopost) → D moves → **A (build_site.py)** → report ฉบับนี้ (HEAD — cc-outbox อยู่นอก ignore-rule → trigger Netlify build เดียวพอดี ตาม logic เดียวกับ media/quotes)
- push ครั้งเดียว = 1 build · hash ล่าสุดดูจาก `git log origin/main -1` หลัง push (report นี้ commit ก่อน push จึงระบุ hash ตัวเองไม่ได้ — CC verify remote==local หลัง push แล้วรายงานในแชท)
- รวม "runlog อื่นถ้ามี": commit state-sync ครอบ `.system_control/content_manifest.json` (threads posted update) + `automation-log/2026-07.jsonl` + `latest.md` ที่ค้างเป็น M อยู่ → status สะอาดฝั่ง tracked

## C. dispatcher ledger fix — ✅ (โครงจริงต่างจากที่ order เดา — อธิบายด้านล่าง)

**ข้อค้นพบ**: ไม่มี dispatcher ตัวเดียวที่โพสต์คลิป — `pipeline/dispatcher.py`/`post_dispatcher.py` เป็นตัววางแผน (ไม่โพสต์) · ตัวโพสต์จริงแยกตามช่อง และตัวที่พลาด ledger เมื่อ 23 ก.ค. คือ **flow browser reels-composer (ฝั่ง Cowork UI — ไม่มีโค้ดใน repo)** · Threads มี ledger append อยู่แล้ว (`tools/threads_ledger.py`)

**สิ่งที่ทำ**:
1. **ใหม่: [tools/clip_ledger.py](../tools/clip_ledger.py)** — appender กลางสำหรับ facebook/youtube/instagram: CLI `py tools\clip_ledger.py --channel facebook --date YYYY-MM-DD` หรือ import `append_row()` · schema ตรง order `{"type":"video","channel":...,"clip_id":...,"text_first80":<หัวข้อจาก manifest>,"ts":ISO+07:00,"source":"cc-dispatcher"}` · UTF-8 no-BOM · **append-only เท่านั้น (open "a" — ไม่มีทาง rewrite)** · dedup ในตัว (channel+date ซ้ำ → DUP_SKIP)
2. **Wire เข้า confirm point ฝั่งโค้ด 2 จุด** (try/except — ledger พังไม่ทำให้ flow โพสต์ล้ม):
   - [tools/yt_upload_batch2.py](../tools/yt_upload_batch2.py) หลังบรรทัด `SUCCESS {date}: videoId=...`
   - [social-autopost/schedule_fb_batch2.py](../social-autopost/schedule_fb_batch2.py) หลัง `SCHEDULED id=...`
3. **Dry-run test ผ่านครบ** (ไม่แตะ ledger จริง — sha1 ก่อน/หลังเท่ากัน `96a26f85...`):
   - dedup จับ row backfill ของ Cowork (fb 23 ก.ค.) → `DUP_SKIP` = เคสบั๊กเดิมจะไม่เกิด double
   - append จริงบน "สำเนา" ledger → **bytes เดิมทั้งไฟล์ identical** + เพิ่ม 1 บรรทัด JSON valid (clip_id `2026-07-23_credit-bureau` + หัวข้อจาก manifest ถูกต้อง) + ไม่มี BOM

**ช่องโหว่ที่เหลือ (ต้องแจ้ง Cowork)**: flow **reels-composer UI ไม่มีโค้ดใน repo ให้ wire** — จุดที่พลาดจริงเมื่อ 23 ก.ค. · มาตรฐานใหม่: ทุกครั้งที่ Cowork ยืนยันคลิปขึ้นช่องทาง UI สำเร็จ ให้รันคำสั่งเดียวจบ:
```
py tools\clip_ledger.py --channel facebook --date 2026-07-XX
```
(dedup ในตัว รันซ้ำได้ปลอดภัย) — เสนอเพิ่มบรรทัดนี้เป็น step ใน runbook ของ reels-composer ฝั่ง Cowork

## D. เก็บกวาด cc-inbox — ✅ ย้าย 15 · เหลือ 4+อ้างอิง

**ย้ายเข้า cc-inbox/done/ (ตรวจจบจริงแล้ว)**: internal-link-boost_20260721 + master_20260723 (สองไฟล์ของงานรอบนี้) · push-fleet-commits_20260721 (105ef5e อยู่บน remote ✓) · gsc-request-indexing_20260718 (จบแผน B + report 69086c9) · CC-ORDER_seo-strike_20260717 + SEO-STRIKE_20260717 (ผ่านตรวจรับ) · ebook trio: APPROVE-push-ebook-launch + RUNBOOK-deploy-ebook-launch + TASK-add-ebook-to-links_20260628 (ebook อยู่ใน build + /links แล้ว) · watermark-fix_20260709 (มี audit/fix-report/removed ครบ) · deploy-debt-funnel_20260709 + ORDER-line-oa-wire + ORDER-pivot-execute (funnel+LINE OA live ทั้งไซต์) · metrics-upgrade_20260702 (traffic-monitor มี GA4 จริงตั้งแต่ 10 ก.ค. = พิสูจน์ว่า wiring สำเร็จ) · REMINDER_ig-reel-schedule-7day_20260711 (หมดอายุ/ถูกแทนด้วย ig-reels workflow)

**ไม่ย้าย — ค้างจริง (ตามกติกา "ไม่จบ = รายงาน ห้ามย้าย ห้ามเริ่มทำเอง")**:
1. 🔴 **order-factfix-20260720.md — P0 ค้าง**: เกณฑ์คลินิกแก้หนี้บนหน้า live ยังผิดทั้งหน้า (ใช้เกณฑ์ "ปิดหนี้ไว ไปต่อได้" 90วัน/1แสน แทนของจริง SAM 120วัน/2ล้าน/อายุ≤70/ผ่อน10ปี) — แก้ 5 จุดใน build_site.py ตามที่ order ระบุ · **แนะนำสั่งรันเป็น order ถัดไปทันที** (กระทบ conversion กลุ่มหนี้แสน-ล้าน = กลุ่มใหญ่สุด) · หมายเหตุ technical: anchor "เดิม" ทั้ง 5 จุดของ order ยังตรงกับโค้ดปัจจุบัน (ลิงก์ .ilinks ที่เพิ่มรอบนี้ไม่ชนจุดใด)
2. CC-ORDER_amplify-winners_2026-07-10 — ตรวจไม่จบในรอบนี้ (ต้องเทียบ GA4/implement จริงหลายข้อ)
3. CC-ORDER_calculator-revenue-wiring_2026-07-10 — น่าจะเสร็จ (calc CTA + GA event มีจริง) แต่หลักฐานยังไม่พอฟันธง
4. REACH-CHANNEL-UNLOCK_fb-open-groups_20260710 — เป็นบันทึกการลงมือของ Cowork มี follow-up ไม่ชัดว่าปิดหรือยัง

**คงไว้ (เอกสารอ้างอิง ไม่ใช่ order)**: CONSULT-90DAY-PLAN · LEVELUP-BRIEF · PANTIP-SPRINT-REPLY-TEMPLATE · REVENUE-STARTER-KIT · REACH-CONTENT-STANDARD_v1 · CC-RESULT_savefirst-eyeball

## ปัญหาที่เจอ + วิธีแก้
- site/ ใน order A สั่ง `git add site/` แต่เป็น ignored build-output → add เฉพาะ build_site.py (Netlify build เอง)
- SEO-STRIKE_20260717.md เป็น untracked (git mv ไม่ได้) → mv ธรรมดา
- dispatcher ตามชื่อใน order ไม่ใช่ตัวโพสต์จริง → recon ทั้ง repo ก่อน แล้ว fix ที่ confirm point จริง + ออก CLI กลางให้ flow UI ใช้
