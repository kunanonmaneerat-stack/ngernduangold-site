# FUNNEL HEALTH — เวรตรวจปลายทางรายสัปดาห์ (31 ก.ค. 2026)

**สถานะรวม: ⚠️ ตรวจไม่ครบ** — หน้าเว็บ/ประตูทางเข้า ✅ ปกติ · แต่ **LINE OA หลังบ้านตรวจไม่ได้ (session หมดอายุ ต้อง login)**

## A. LINE OA @804qodya — ⏭️ ข้ามทั้งหมด (login wall)

`manager.line.biz` redirect → `account.line.biz/login` (หน้า LINE Business ID) ทั้งใน Chrome MCP และใน Chrome window ปกติ (non-CDP, ตามสูตร OPERATING-NOTES ข้อ 2) = **session หมดจริง ไม่ใช่ bot-detection**
กฎเหล็กของเวรนี้: ห้ามกรอกรหัสผ่าน → จึงข้าม ไม่เดา

| # | ข้อตรวจ | สถานะ | หมายเหตุ |
|---|---|---|---|
| A1 | แชท (chat) = เปิด | ⏭️ ตรวจไม่ได้ | **จุดตายที่เคยเกิด 25 ก.ค. — ยังยืนยันไม่ได้รอบนี้** |
| A2 | วิธีตอบ = แมนนวล + ตอบกลับอัตโนมัติ | ⏭️ ตรวจไม่ได้ | |
| A3 | เวลาตอบข้อความ (business hours) = ปิด | ⏭️ ตรวจไม่ได้ | |
| A4 | auto-reply 2 ตัว "กำลังใช้งาน" | ⏭️ ตรวจไม่ได้ | `ขอจดหมาย-leadmag` + `Default` |
| A5 | ข้อความทักทายเพื่อนใหม่ = เปิด | ⏭️ ตรวจไม่ได้ | |
| A6 | Rich menu "เมนูหลัก" = แสดงอยู่ | ⏭️ ตรวจไม่ได้ | |
| A7 | friends ปัจจุบัน | ⏭️ ตรวจไม่ได้ | baseline = **1 คน** (25 ก.ค. 2026) · delta ยังวัดไม่ได้ |

## B. ประตูทางเข้า — ✅

`https://line.me/R/ti/p/@804qodya` → HTTP 200 · หน้า "Add LINE friend" ของ OA ขึ้นปกติ พร้อม QR `M_804qodya_GW.png` · **ไม่ใช่ 404 / ไม่พบบัญชี** · `page.line.me/804qodya` → 200 ด้วย

## C. หน้าเงินบนเว็บ — ✅ ครบทุกหน้า

| หน้า | HTTP | CTA LINE | เส้นทางจ่าย |
|---|---|---|---|
| /links | 200 (47 KB) | ✅ @804qodya x4 | ✅ 199฿ + 59฿ · Gumroad 2 ลิงก์ (utm ครบ) |
| /debt-letter-kit | 200 (18 KB) | ✅ @804qodya x3 | ✅ หน้าขาย 199฿ |
| /debt-health-check | 200 (28 KB) | ✅ @804qodya x1 | — (หน้า lead) |

**Gumroad (เส้นทางจ่ายด้วยบัตร) = ✅ live ทั้ง 2 ตัว** (ดูอย่างเดียว ไม่ได้กดซื้อ):

- `gumroad.com/l/debt-toolkit` → 200 · ชุดเครื่องมือปลดหนี้ (Excel) · ไม่พบ marker unpublished
- `gumroad.com/l/debt-payoff-planner` → 200 · แผนปลดหนี้บัตรเครดิต · ไม่พบ marker unpublished

## D. คำต้องห้ามบนหน้าเงิน — ✅ สะอาด

grep หน้า /links และ /debt-letter-kit:

| คำ | ผล |
|---|---|
| อนุมัติไว | **0 — ✅ ถูกแก้แล้ว** (order ที่ค้างตั้งแต่ 25 ก.ค. ปิดได้) |
| อนุมัติแน่นอน / ไม่เช็คบูโร / รับรองผล / ดอกเบี้ย 0% ตลอด | 0 — ✅ |
| การันตี | พบ 5 ครั้ง แต่ **ทุกครั้งเป็น "ไม่การันตี..." = disclaimer ที่ถูกต้อง** — ✅ ไม่นับเป็น flag |

บริบทที่เจอ: ไม่การันตีการเคลม · ไม่การันตีอนุมัติ/อัตราดอกเบี้ย · ไม่การันตีการอนุมัติ · ไม่การันตีผลลัพธ์

## ต้องกดอะไร ที่ไหน

**สิ่งเดียวที่ต้องทำ: login ที่ https://manager.line.biz/ → เข้าบัญชี @804qodya → ดู การตั้งค่า > แชท ว่าเป็น "เปิด" อยู่**

(เปิดหน้าต่าง Chrome ค้างไว้ที่หน้า login แล้ว — กดเข้าได้เลย)

เหตุผล: เครื่องเฝ้าปลายทางตัวนี้พึ่งเซสชันเบราว์เซอร์ของเจ้าของ — ถ้า session หมด เวรกนี้ก็ตาบอด คือจุดตายเดิมกลับมาได้โดยไม่มีใครรู้
ข้อเสนอเชิงระบบ: หลัง login ครั้งหน้า ให้ติ๊ก "จดจำการเข้าสู่ระบบ" / keep me signed in เพื่อให้เวรอัตโนมัติตรวจได้ต่อเนื่อง

---
*รอบแรกของเวร funnel-endpoint-check · read-only · ไม่มีการแก้ค่าใดๆ · ไม่มี PII ลูกค้าในไฟล์นี้*
