# 🌊 FLOW รวมปัจจุบัน — ngernduangold (21 มิ.ย. 2026)

ระบบ multi-agent ที่ **Cowork คุมหัว** · สมองฟรี (GLM→Qwen) · แจ้ง Telegram · ทุกอย่างเป็น draft (คนกดโพสต์)

## A. รอบประจำวัน (อัตโนมัติ — Windows task 07:30)
```
1. orders.txt ───────────────► หัวข้อวันนี้ (Cowork ตั้ง / default 5)
2. dispatcher.py ────────────► วนแต่ละหัวข้อเข้าสภา
3. content_council.py (5 ชั้น) ► expert → comply → value → review → correct
                                 = คำตอบ value-first ตรวจแล้ว → council-<วันที่>.md
4. ► ping Telegram "คิวพร้อม approve N รายการ"
5. เจ้าของเปิดคิว → เลือก → โพสต์เอง (Pantip/Threads/FB)   ← จุดเดียวที่ใช้คน
```

## B. รอบ execute + creative (สั่งเมื่อต้องการ)
```
loop.py plan:
  6. executor.py ────────────► รวมคำตอบที่ผ่านสภา = work order → cc-inbox/
  7. cc_bridge.send ─────────► ping CC ว่ามีงาน
  8. Claude Code execute ────► คอนเทนต์หลายแพลตฟอร์ม → cc-outbox/
        (CC ไม่ว่าง → cc_runner.py ทำแทนด้วย free_llm)
  9. comply_gate.py ─────────► ตรวจ trigger/caveat ซ้ำ + auto-fix + stamp ✅/⚠️
loop.py collect:
 10. report_to_cowork.py ────► รวมผล (มี verdict) → cowork-review/ → ping Cowork

head_creative.py (ขนานกัน):
  • platform_agents (6): Pantip·FB·IG·TikTok·YouTube·Threads → คอนเทนต์+แทคติกเฉพาะแพลตฟอร์ม
  • รวม → creative-brief-<ts>.md (แต่ละตัวมี comply verdict) → ping Cowork
```

## B2. โซเชียลออปส์ รายแพลตฟอร์ม (หลัง head_creative)
```
 social_queue.py ──────────► 6 agent รับคิว draft แยก social-queue/<platform>/
 post_review.py ───────────► 6 agent QA ก่อนโพสต์ (เช็กลิสต์เฉพาะแพลตฟอร์ม) -> READY/FIX
 head_social_review.py ────► รวมสถานะ 6 แพลตฟอร์ม -> ping Cowork (เลือก READY / ส่ง FIX กลับ)
 post_prepare.py ──────────► แพ็กพร้อมวาง (เวลาโพสต์+เช็กลิสต์) เฉพาะตัว READY
                              -> post-ready/<platform>/  ** คนกด publish เอง ไม่ auto-post **
```

## C. ยกระดับไอเดีย (เมื่ออยากได้ของคุณภาพสูง)
```
 idea_council.py ───────────► 4 มุมมอง (growth/viral/trust/funnel) → ideas-<ts>.md
 external_consult.py ───────► บรีฟ → Cowork ขับ Chrome ถาม Gemini 3.5 + Opus 4.8
                              → รวม consult-merged-<ts>.md → Cowork สังเคราะห์
```

## D. ตัวคุม + เซฟตี้ (พันรอบทุกสเตจ)
```
 ask_cowork.py ─────────────► agent ไหนไม่แน่ใจ → เขียนคำถาม + ping → Cowork ชี้ขาด
 COWORK ────────────────────► วิเคราะห์ทุก review/brief → ยกระดับ → เขียน orders.txt รอบใหม่ → วน A
 กฎเหล็ก: ไม่โพสต์/commit/deploy เอง · ลิงก์ DM/bio · ตัวเลขเป็นช่วง+เช็กธนาคาร · คนอนุมัติของจริง
```

## วงปิด (ภาพเดียว)
orders → dispatcher → **สภา 5 ชั้น** → (executor → CC → comply gate → report) + (6 platform → head creative) → **Cowork วิเคราะห์** ↑ เสริมด้วย idea_council / Gemini+Opus, ถ้าไม่แน่ใจ → ask_cowork → **วนกลับ orders**
