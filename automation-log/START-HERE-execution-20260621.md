# 🎯 START HERE — แผนลงมือ 15 นาที/วัน (front-door เดียว)

> สรุปจากข้อมูลจริง (IG+FB 30 วัน): คุณ**โพสต์ขยัน + คอนเทนต์ดี**อยู่แล้ว แต่ทุกโพสต์ reach/engagement ≈ 0
> **คอขวดคือ "reach" ไม่ใช่คอนเทนต์** → เลิกเพิ่มจำนวนโพสต์ไปหา 0 follower · ย้ายแรงไป "ยืม reach" จากที่ที่มีคนอยู่แล้ว
> ระบบหลังบ้าน (auto-DM, วัดผล, เว็บ, คอนเทนต์) ผมทำครบแล้ว เหลือ "ลงมือฝั่งคนดู" ที่ต้องใช้บัญชีคุณ

---

## ✅ ตั้งค่าครั้งเดียว (ทำวันนี้ ~20 นาที)
- [ ] **Deploy เว็บ** (ปิด editor ที่เปิด build_site.py ก่อน!) → `py build_site.py` (ต้องได้ "quiz.html written") → `git add -A && git commit -m "seo+polish" && git push`
- [ ] **เทสต์ auto-DM** — คอมเมนต์ "เช็กสิทธิ์" ใต้ Reel IG ตัวเอง → ดู DM เด้ง (CreatorFlow live แล้ว)
- [ ] **ใส่ลิงก์ /quiz** ในไบโอ IG + โปรไฟล์ Pantip + (เพจ FB ปุ่ม "ดูเพิ่มเติม")

## 🔁 รูทีนรายวัน 15 นาที (เรียงตาม ROI — reach สูงสุดก่อน)

### 1) Pantip (7 นาที) — มีคนอ่านจริงทุกวัน ⭐ ลีเวอร์ #1
- เปิด [tag/รวมหนี้](https://pantip.com/tag/รวมหนี้) + [tag/บัตรเครดิต](https://pantip.com/tag/บัตรเครดิต) → หากระทู้ "ถาม" ใหม่ → ตอบ value-first (สคริปต์ใน `post-ready/PANTIP-pack-20260621.md`) ลิงก์อยู่ในโปรไฟล์ ห้ามแปะลิงก์ในเนื้อ
- กระทู้ถามสดตอนนี้: [43954326 รวมหนี้ เซลล์ 30k](https://pantip.com/topic/43954326) · [42954624](https://pantip.com/topic/42954624) · [41097350](https://pantip.com/topic/41097350)
- **สัปดาห์ละ 1 ครั้ง:** ตั้งกระทู้ value ของตัวเอง (สคริปต์ "เป็นหนี้บัตร 3-4 ใบ ทำตารางเทียบ..." ใน pack) — กระทู้ Pantip ติด Google ยาว = ได้ทราฟิกต่อเนื่อง

### 2) ยืม reach เพจใหญ่ (4 นาที)
- ไปคอมเมนต์ **ช่วยจริง** (ไม่สแปมลิงก์) ใต้โพสต์ล่าสุดของเพจการเงินใหญ่ — คนเห็นชื่อเรา → คลิกโปรไฟล์ → เจอลิงก์
- เป้า: [The Money Coach](https://www.facebook.com/TheMoneyCoachTH) · [KBank Live](https://www.facebook.com/KBankLive) · ลงทุนแมน · aomMONEY · Krungsri (ค้น "เพจการเงิน")
- เทคนิค: ตอบเสริมความรู้/ประสบการณ์จริงในคำถามที่คนคอมเมนต์ถาม = ดูเป็นกูรู ไม่ใช่ขายของ

### 3) โพสต์ของตัวเอง (4 นาที) — แก้ CTA ให้เชื่อม funnel
- Reel/โพสต์ใหม่ ใช้ปฏิทิน `post-ready/REEL-hooks-7day-20260621.md` (ฮุกไม่ซ้ำ)
- **เปลี่ยน CTA ทุกอัน: "ลิงก์ในไบโอ" → "คอมเมนต์ เช็กสิทธิ์ เดี๋ยวส่งให้ทาง DM"** (IG) = ป้อน auto-DM
- ใส่ 5–10 แฮชแท็กตรงกลุ่มทุกครั้ง · อย่าใช้แคปชันซ้ำ (ข้อมูลจริง: ซ้ำ = รีชตก ~10×)

## 📊 วัดผล (ไม่ต้องทำเอง)
- **Growth Control Panel** (แถบข้าง Cowork) — เปิดดู reach/คอมเมนต์สด
- **ทุกจันทร์ 09:04** — ผมสรุป IG อัตโนมัติส่งให้ (`ig-weekly-pulse`)
- เช็ก **DMs sent** ที่ [app.creatorflow.so](https://app.creatorflow.so) ขยับจาก 0 = funnel เริ่มวิ่ง

## 🧭 ตัวชี้วัดว่ามาถูกทาง (ลำดับที่ควรเห็น)
reach ขึ้น (จาก Pantip/เพจใหญ่) → **คอมเมนต์เริ่มเข้า** → DMs sent > 0 → quiz starts (GA4) → affiliate clicks
ถ้าคอมเมนต์ยัง 0 = ยังไม่ได้แก้ CTU/reach ตามข้างบน

---
### คลังไฟล์ (อ้างอิงเวลาทำแต่ละสเต็ป)
- คอนเทนต์ Reel: `post-ready/REEL-hooks-7day-20260621.md`
- โพสต์ FB/IG/Pantip: `post-ready/PACK-feed-autodm-20260621.md`
- Pantip thread+replies: `post-ready/PANTIP-pack-20260621.md`
- วินิจฉัย IG: `ig-insights-real-20260621.md` · auto-DM: `AUTO-DM-SETUP-2026-06-21.md` · เว็บ: `cc-outbox/result-website-polish-20260621.md`
