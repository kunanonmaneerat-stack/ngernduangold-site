# 📅 เตือนรายสัปดาห์ — คิว IG Reel ล่วงหน้า 7 วัน
รันอัตโนมัติ: **จันทร์ 20 ก.ค. 2026 21:32 ICT** · รอบก่อนหน้า: `REMINDER_ig-reel-schedule-7day_20260711.md`

## ⚡ TL;DR
- **คิว IG = 6 วันล่วงหน้า** (21–26 ก.ค.) — ยังไม่ขาด แต่**ชนกำแพงวันที่ 27 ก.ค.**
- **ไม่ขาดไฟล์เลย** ✅ batch3 (27 ก.ค.–2 ส.ค.) เรนเดอร์ครบ 7 คลิป 1080x1920 + โฮสต์บนเว็บแล้ว + แคปชันพร้อม → **ไม่ต้องถ่าย/ตัดอะไรใหม่**
- **จุดขาดจริง = `reels/schedule.json` มีถึงแค่ 26 ก.ค.** ตัวรันอัตโนมัติจะหา entry วันที่ 27 ไม่เจอ → เงียบ ไม่โพสต์
- **ทำอย่างเดียว:** เอา JSON 7 บรรทัดที่เตรียมไว้ merge เข้า `reels/schedule.json` แล้ว commit → คิวยาวถึง 2 ส.ค. ทันที
  - ไฟล์พร้อมวาง: `automation-log/cc-inbox/PATCH_ig-schedule-batch3_20260720.json`
- วันนี้ (20 ก.ค.) โพสต์แล้ว ✅ 19:01 ICT — และเป็น**วันแรกที่หลุดจากบั๊ก 720p** ที่ FAIL ติดกัน 16–19 ก.ค.

---

## 📊 สถานะคิวจริง (ยืนยันสดผ่าน Meta API วันนี้)
| | |
|---|---|
| โพสต์ล่าสุด | [reel/DbA5FIdDMbF](https://www.instagram.com/reel/DbA5FIdDMbF/) — "เช็กสุขภาพหนี้ 60 วิ" · 20 ก.ค. 19:01 ICT |
| ความสม่ำเสมอ | **7/7 วันติด** (14–20 ก.ค.) ยิงตรงเวลา 19:00–19:01 ICT ทุกวัน ไม่มีวันหลุด |
| คิวล่วงหน้าใน `schedule.json` | **21–26 ก.ค. = 6 วัน** (ครบทั้ง file + caption) |
| คิวหลังจากนั้น | **27 ก.ค. เป็นต้นไป = ว่าง** ⚠️ |

> กลไกที่ทำงานอยู่จริง = GitHub Action `ig-reels` → Instagram Content Publishing API (Graph API ทางการของ Meta)
> — **ไม่ใช่บอทนอกระบบแบบ Postiz** จึงไม่ผิดการ์ดเรลกัน shadowban · อ่าน `RUNBOOK_ig-reels-api_20260711.md`

---

## ✅ ไฟล์ batch3 พร้อมครบ — ตรวจแล้วทุกจุด
ทุกไฟล์ผ่าน: มีจริงบน `site/reels/` (โฮสต์แล้ว) · แคปชันมี `ข้อมูลเพื่อการศึกษา` + `ผลิตด้วย AI` ครบ (ด่าน fail-closed ของ `ig_publish.py`)

| วันที่ | ไฟล์ | หัวข้อ |
|---|---|---|
| จ 27 ก.ค. | `2026-07-27_b3-01.mp4` | รถยังผ่อนไม่หมด จำนำได้ไหม |
| อ 28 ก.ค. | `2026-07-28_b3-02.mp4` | รถเกิน 20 ปี เข้าไฟแนนซ์ได้ไหม |
| พ 29 ก.ค. | `2026-07-29_b3-03.mp4` | รถเกิน 25 ปี เข้าไฟแนนซ์ได้ไหม |
| พฤ 30 ก.ค. | `2026-07-30_b3-04.mp4` | รถอายุ 15 ปี จัดไฟแนนซ์ได้ไหม |
| ศ 31 ก.ค. | `2026-07-31_b3-05.mp4` | เงินเดือน 30k วงเงินบัตรดูยังไง |
| ส 1 ส.ค. | `2026-08-01_b3-06.mp4` | บัตรเครดิตเงินเดือน 30k เลือกแบบไหน |
| อา 2 ส.ค. | `2026-08-02_b3-07.mp4` | เงินเดือน 30k ได้วงเงินเท่าไหร่ |

---

## 🔧 วิธีปิดช่องว่าง (ทางหลัก — 2 นาที)
1. เปิด `automation-log/cc-inbox/PATCH_ig-schedule-batch3_20260720.json`
2. merge 7 key นั้นเข้า `reels/schedule.json` (โครงสร้างเดียวกันเป๊ะ: `file` / `slug` / `caption`)
3. commit + push (งาน CC — Cowork ไม่แตะ git ตามการ์ดเรล)
4. เสร็จ → คิวยาวถึง **2 ส.ค.** ไม่ต้องทำอะไรอีก cron ยิงเองทุก 19:00 ICT

> **แก้ให้แล้วในไฟล์ patch:** แคปชัน batch3 ใน `content_manifest.json` มี `\n` เป็น**ตัวอักษรจริง** (จะโผล่เป็น "\n\n" ให้คนอ่านเห็น) — patch แปลงเป็นขึ้นบรรทัดใหม่จริงเรียบร้อย ถ้าแก้ที่ manifest ต้นทางด้วยจะดีกว่า

## 🖱️ ทางสำรอง — Business Suite Planner (ถ้าไม่อยากแตะ JSON)
1. เปิด business.facebook.com → เมนู **"แพลนเนอร์"** หรือปุ่ม **"สร้างคลิป Reels"**
2. อัปโหลดคลิป batch3 จาก `reels/` (รีไซเคิลจาก TikTok ได้)
3. วางแคปชันจาก patch ไฟล์ + **disclosure ครบ** · ห้ามตัวเลข/การันตีดอก
4. เปิดสวิตช์ **"กำหนดวันที่และเวลา"** → 19:00 (ให้ตรงจังหวะเดิมที่ยิงอยู่)
5. ทำครบ 7 ไฟล์ → กด **"กำหนดเวลา"**
> ⚠️ ถ้าใช้ทางสำรองนี้ **ต้องไม่ใส่ entry ลง `schedule.json` ด้วย** ไม่งั้นจะโพสต์ซ้ำ 2 ที่วันเดียวกัน

---

## 🔎 จุดต้องเช็ก (ไม่บล็อกงาน แต่ควรดู)
1. **`affiliate: false` ทั้ง 7 คลิป batch3** — แต่แคปชันชวนไป "ตัวช่วยเทียบ ลิงก์ในไบโอ" ซึ่ง bio → `ngernduangold.com/links` (มีลิงก์พันธมิตร) · batch1/2 ที่ใช้ pattern เดียวกันตั้ง `affiliate: true` + ใส่ "มีลิงก์พันธมิตร" → **น่าจะติดป้ายผิด เจ้าของตัดสิน** ถ้าใช่ ต้องเติม "· มีลิงก์พันธมิตร" ทุกแคปชัน
2. **disclosure batch3 สั้นกว่าเดิม** — ใช้ `ข้อมูลเพื่อการศึกษา · ผลิตด้วย AI` ขาด "ไม่ใช่คำแนะนำทางการเงิน" ที่ batch1/2 มีครบ 100% · ผ่านด่าน script แต่หลุดฟอร์แมตบ้าน → แนะนำเติมให้เหมือนเดิม
3. **`content_manifest.json` สถานะ 20–26 ก.ค. ยังเป็น `Rendered`** ทั้งที่ 20 ก.ค. โพสต์ไปแล้วจริง — สถานะไม่อัปเดตอัตโนมัติ (cosmetic drift) ระวังอ่านผิดว่ายังไม่ได้โพสต์
4. **`automation-log/ig-reels/` ว่างเปล่า** — ไม่มี `published.json` / `log-<date>.md` ทั้งที่โพสต์สำเร็จ 7 วันติด → น่าจะเป็น workspace ที่ mount ยังไม่ได้ pull commit ของบอท (หรือ step commit ล้ม) · **ผลข้างเคียง: ตัวกันโพสต์ซ้ำ (dedup guard) อาจไม่ทำงาน** ถ้า Action ถูก re-run — ควรให้ CC ยืนยันว่าฝั่ง origin มีไฟล์นี้จริง

5. **working tree มีของค้างไม่ commit** — `reels/schedule.json` (แก้ 11 ก.ค.) และ `content_manifest.json` (แก้ 19 ก.ค.) ยังไม่ถูก commit · ตรวจแล้ว = **เปลี่ยนแค่การจัดย่อหน้า ไม่กระทบเนื้อหา** (HEAD กับ working copy เหมือนกันทุกประการ 16 คีย์ 11–26 ก.ค.) → merge patch ทับได้ปลอดภัย แต่ CC ควรเคลียร์ของค้างตอน commit ด้วย

## 🚧 การ์ดเรลที่เคารพในรอบนี้
- ไม่โพสต์เอง ไม่แตะคิวสด ไม่ push git · อ่านอย่างเดียว + เตรียมไฟล์ให้
- ตรวจสถานะสดผ่าน Meta Graph API (read-only) ไม่ใช้บอทนอกระบบ
