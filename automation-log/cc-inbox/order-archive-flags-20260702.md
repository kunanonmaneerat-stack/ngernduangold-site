# ORDER — archive ธงที่ปิดแล้ว (2 ก.ค. 2569, จาก Cowork)
> Cowork เคลียร์งานตกค้างครบ 4 ตัวแล้ว — CC แค่ commit หลักฐาน + เอาธงออกจาก handoff/backlog (ไม่ต้องทำงานเพิ่ม)
> กฎเดิม: ห้ามแตะ build_site.py (Stitch defer) · ห้าม push ถ้าไม่จำเป็น

## commit ไฟล์ที่ Cowork สร้าง/แก้:
- `automation-log/DECISION-stitch-fold_20260702.md` (มติ DEFER fold — เหตุผล GA4)
- `automation-log/gumroad-sales.csv` (ตรวจแล้ว 0 orders + note)
- `automation-log/launch-status.json` (pending อัปเดต 4 ตัวเป็น ✅)
- order นี้

## archive ออกจาก PROJECT-HANDOFF / backlog (ปิดแล้ว ไม่ใช่ TODO):
1. **Stitch fold** → DEFER ถาวรจนกว่าจะเข้าเงื่อนไข revisit ใน DECISION-stitch-fold (reach ขึ้น + homepage มี conv intent) · เก็บ preview-home.html ไว้ ไม่ลบ
2. **CreatorFlow DM fix** → เสร็จ (ลิงก์ .com/quiz + once-per-person เปิด) · หมายเหตุลง handoff: timer-delay ≥30วิ เป็นฟีเจอร์ Pro — แผน Free ทำไม่ได้ ไม่ต้องตามอีก
3. **TikTok e-book** → โพสต์แล้วผ่านเว็บ (ไม่ต้องรอมือถือ)
4. **Gumroad CSV** → ช่องทางตรวจ setup แล้ว (0 ยอดปัจจุบัน)

## เหลือจริงในระบบ (ยังเปิด):
- Pantip FROZEN ถึง ≥16 ก.ค. (เจ้าของกด 'รับทราบ' เอง)
- Flow 8 prompt ที่เหลือ (รอ backend หาย + assembly ตาม order-flow-assembly)
- คิว FB/Threads 3-8 ก.ค. (Threads auto, FB Cowork ยืนยัน)
