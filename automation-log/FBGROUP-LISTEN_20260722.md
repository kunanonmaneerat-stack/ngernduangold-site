# FBGROUP-LISTEN — 22 ก.ค. 2026 (Cowork · ~10:24, รันแบบ scheduled)

## สรุปสั้น (ภาษาไทย)
- **Identity ที่ใช้:** ❓ ตรวจไม่ได้ — เปิดเบราว์เซอร์ไม่ได้เลยในรอบนี้ (ห้ามสรุปว่า identity ผิด)
- **กลุ่มที่เลือกวันนี้:** ยังเปิดไม่ถึง (ทาบไว้ต่อจาก 21 ก.ค. = **รวมพลคนอยากปิดหนี้ รวบหนี้** id 2145569355698267)
- **จำนวนร่าง:** 0
- **สัญญาณ mod:** ไม่พบ (ตรวจไม่ได้เพราะเปิดกลุ่มไม่ได้ — ไม่ใช่การถูกลงโทษ · ledger สะอาด ยังไม่มี entry ถูกลบ/เตือน/เตะ)
- **ผลลัพธ์:** 🔴 **บล็อกที่ชั้นเบราว์เซอร์** — ทั้งสองช่องทางใช้ไม่ได้ในการรัน scheduled ต้องให้เจ้าของรันแบบ interactive

---

## 🔴 สาเหตุที่หยุด — เบราว์เซอร์ใช้ไม่ได้ทั้งสองทาง
งานนี้ต้องอ่านฟีดกลุ่ม Facebook ผ่านเบราว์เซอร์ (นำทางด้วย URL + รัน JS เช็ก identity/visibilityState) แต่รอบนี้ **ไม่มีช่องทางเบราว์เซอร์ให้ใช้เลย:**

1. **Claude in Chrome extension — ไม่ได้เชื่อมต่อ** (ยืนยัน 3 ครั้ง: `list_connected_browsers` = ว่าง, `tabs_context` แจ้ง "not connected")
   - นี่คือเครื่องมือที่รอบก่อนๆ ใช้ (อ่าน `document.body.innerText`, เช็ก `visibilityState`, `resize_window`)
2. **computer-use (คุมเดสก์ท็อปตรง) — อนุมัติไม่ได้ในรัน scheduled**
   - ระบบตอบกลับตรงๆ: *"Computer-use access to Google Chrome can't be approved during a scheduled run"*
   - อีกทั้งเบราว์เซอร์เปิดได้แค่โหมด read (ดูอย่างเดียว คลิก/พิมพ์/นำทาง/สลับ identity ไม่ได้ · รัน JS ไม่ได้)

→ เปิด facebook.com ไม่ได้ → **ด่าน 0 (identity) และ ด่าน 0.5 (tab visible) รันไม่ได้ตั้งแต่ต้น**
→ ตามกฎ SKILL **ห้ามสรุปว่า identity ผิด และห้ามสรุปว่ากลุ่มไม่มีโพสต์ที่ตอบได้เด็ดขาด** — จึงหยุดและรายงานตามนี้

## 🔁 นี่คือรูปแบบเดิมที่ซ้ำ 3 วันติด (เชิงโครงสร้าง)
| วันที่ | ผล | สาเหตุชั้นเบราว์เซอร์ |
|---|---|---|
| 20 ก.ค. | 0 ร่าง | Chrome ค้างโหมดเพจผิด + สับสนสถานะสมาชิก |
| 21 ก.ค. | 0 ร่าง | background-tab wall (ฟีดโหลดไม่ครบ) |
| 22 ก.ค. | 0 ร่าง | เบราว์เซอร์ใช้ไม่ได้ทั้งสองทางในรัน scheduled |

**ข้อสังเกตเชิงยุทธศาสตร์:** งานนี้ต้องมี "เบราว์เซอร์ที่เชื่อมต่อ + แท็บอยู่หน้าจอ (active/visible) + คนอยู่หน้าเครื่อง" ซึ่งการรันแบบ scheduled (ไม่มีคน) ให้ไม่ได้โดยธรรมชาติ → ควรเปลี่ยนเป็นงาน **เจ้าของกดรันเอง (semi-manual)** แทน cron อัตโนมัติ ไม่งั้นจะล้มด้วยเหตุเดิมทุกวัน

## ✅ สิ่งที่ทำได้ในรอบนี้ (ไม่แตะเบราว์เซอร์)
- อ่านนโยบาย: `FB-GROUPS-SHORTLIST_20260719.md` + SKILL
- grep `post-ledger.jsonl` `channel=fb-group` → **มีคอมเมนต์จริงกลุ่มเดียว = วิถีออมเงินมนุษย์เงินเดือน (19 ก.ค.)** · อีก 6 กลุ่มยังไม่เคยคอมเมนต์
- ทวนรอบหมุนเวียน: 21 ก.ค. ทาบ "รวมพลคนอยากปิดหนี้ รวบหนี้" ไว้แต่ยังไม่สำเร็จ (tab wall) → **รอบหน้าเริ่มกลุ่มนี้ก่อน**
- **ไม่ได้คอมเมนต์ · ไม่ได้โพสต์ · ไม่ได้กรอกรหัส/2FA · ไม่แตะปุ่มอะไรในกลุ่ม**

## 🎯 สิ่งที่เจ้าของต้องทำ เพื่อให้รอบหน้าเดินได้
1. เปิด **Google Chrome** และเชื่อม **Claude in Chrome extension** (ล็อกอินบัญชีเดียวกับแอปนี้)
   - ลิงก์ extension: https://chromewebstore.google.com/detail/fcoeoabgfenejglbffodgkkbkcdhcgfn
2. เปิดแท็บ **facebook.com** ค้างไว้ให้เป็น **แท็บ active/หน้าจอ** (visibilityState = visible) — ยืนยัน identity เป็น **เพจ เงินเดือนสมองทอง**
3. สั่งรันงานนี้ซ้ำ **ในแชต (interactive) ตอนนั่งอยู่หน้าเครื่อง** — เครื่องจะ: ตรวจ identity → เปิด "รวมพลคนอยากปิดหนี้ รวบหนี้" → อ่าน ~15 โพสต์ → หาโพสต์ครบ 4 เกณฑ์ (เช็กคอมเมนต์เปิดด้วย) → ส่งร่าง ≤3 ให้เคาะ

> ทางเลือก: ถ้าอยากให้รัน scheduled ได้จริง ต้องเพิ่ม Google Chrome เข้า settings ของ scheduled task (computer-use) — แต่โหมดนั้นเป็น read-only + ยังติดเงื่อนไข tab-visible อยู่ดี **การรัน interactive จึงเชื่อถือได้กว่ามาก**

## Ledger
❌ **ไม่ append** — ไม่มีคอมเมนต์เกิดขึ้น (append เฉพาะหลังเจ้าของอนุมัติและโพสต์จริงเท่านั้น)
