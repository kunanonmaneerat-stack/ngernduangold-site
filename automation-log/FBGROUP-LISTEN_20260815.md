# FBGROUP-LISTEN 15 ส.ค. 2026 — ❌ ไม่สำเร็จ (usage limit ตัดกลางทาง — วันที่ 2 ติดกัน)

## สถานะ: หยุดก่อนถึงด่าน 0 (ยังไม่ได้เปิด Facebook)

### ทำไปแล้ว
- ✅ อ่านนโยบาย FB-GROUPS-SHORTLIST_20260719.md
- ✅ ตรวจไฟล์ FBGROUP-LISTEN ย้อนหลัง — ล่าสุดที่มีเนื้อหาจริงคือ **10 ส.ค.**
- ✅ อ่าน FBGROUP-LISTEN_20260814.md — ยืนยันว่ารอบ 14 ส.ค. ก็ล้มด้วยสาเหตุเดียวกัน

### ยังไม่ได้ทำ
- ❌ ด่าน 0 — ตรวจ identity เพจ "เงินเดือนสมองทอง"
- ❌ อ่านร่างค้างใน FBGROUP-LISTEN_20260810.md
- ❌ grep post-ledger หากลุ่มที่ยังไม่เคยคอมเมนต์
- ❌ เปิดกลุ่ม / อ่านฟีด / ร่างคำตอบ

### ⚠️ ไม่มีข้อสรุปใดๆ เรื่องกลุ่ม
ห้ามตีความไฟล์นี้ว่า "ไม่มีโพสต์ที่ตอบได้" หรือ "กลุ่มมีปัญหา" — **ไม่ได้ตรวจ**

---

## 🔴 สัญญาณที่ต้องแก้ระดับโครงสร้าง (ไม่ใช่แค่รันใหม่)

**ช่องว่างสะสม: 11, 12, 13, 14, 15 ส.ค. = 5 วันติดที่ไม่มีร่างใหม่ออกมาเลย**
- 14 ส.ค. ล้มเพราะ usage limit
- 15 ส.ค. ล้มเพราะ usage limit (ซ้ำจุดเดิม — ยังไม่ทันเปิดเบราว์เซอร์)

นี่คือคอขวด**คนละตัว**กับบทเรียน 26 ก.ค. (ตอนนั้น = ผลิตร่างได้แต่ไม่ถูกส่งมอบ)
รอบนี้ = **ผลิตไม่ออกเลย** เพราะงานเตรียมข้อมูลกินโควตาหมดก่อนถึงงานจริง

### ข้อเสนอแก้ (ให้เจ้าของเคาะ)
1. **ตัดขั้นเตรียมให้สั้นลง** — งานอ่านนโยบาย + ตรวจ log ย้อนหลัง + grep ledger กินไปหลายรอบก่อนถึง Facebook
   → ย่อเหลือ: อ่านไฟล์ FBGROUP-LISTEN ล่าสุด **ไฟล์เดียว** แล้วเปิดกลุ่มทันที
2. **เลื่อนเวลารันไปช่วงที่โควตายังไม่ถูกใช้** (ตอนนี้ชนตอนที่โควตาเหลือน้อยแล้ว)
3. **ลดขอบเขตต่อรอบ** — ตั้งเป้า 1 ร่าง/รอบ ไม่ใช่ ≤3 เพื่อให้จบภายในโควตาที่มี

---

## สิ่งที่ควรทำรอบหน้า (ตามลำดับ ห้ามสลับ)
1. เปิด `FBGROUP-LISTEN_20260810.md` **เป็นอย่างแรก** — ถ้าร่างเดิมยังไม่ถูกโพสต์และโพสต์เป้าหมายยัง active + คอมเมนต์เปิด → **เสนอร่างเดิมซ้ำ ไม่ต้องร่างใหม่**
2. ด่าน 0 (identity เพจ เงินเดือนสมองทอง)
3. เปิดกลุ่ม → ร่าง → ส่งเข้าแชต
