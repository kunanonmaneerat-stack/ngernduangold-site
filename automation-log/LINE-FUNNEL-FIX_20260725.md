# 🔴→✅ LINE FUNNEL FIX — เจอ + แก้จุดตายของช่องทางขาย (25 ก.ค. 2026 · Cowork)

## จุดตายที่เจอ (audit หลังเจ้าของ log in ให้)
**แชท (chat) = ปิด** ที่ LINE OA @804qodya — คนที่กดจากเว็บ/โพสต์เข้ามาแล้ว "ทักไม่ได้"
→ North Star ทั้งหมด (ชุดจดหมาย 199฿ · คู่มือ 59฿) ขายผ่าน LINE เป็นหลัก = ปลายทางของทุก CTA ตัน
→ อธิบายได้ว่าทำไม **friends = 1 คน** ทั้งที่โพสต์+traffic เดินมาหลายสัปดาห์

## แก้แล้ว (ยืนยันบนเซิร์ฟเวอร์ reload แล้ว)
| รายการ | ก่อน | หลัง |
|---|---|---|
| แชท | 🔴 ปิด | ✅ เปิด |
| เวลาตอบข้อความ (business hours) | เปิด (นอกเวลาเท่านั้นที่ auto ตอบ) | ✅ ปิด = ไม่จำกัดเวลา |
| วิธีตอบข้อความ | แชทแบบแมนนวล (auto-reply ไม่ทำงาน) | ✅ **แชทแบบแมนนวล + ข้อความตอบกลับอัตโนมัติ** |
| สถานะ auto-reply | หยุดใช้งาน | ✅ "กำลังใช้งาน — จะถูกส่งไปยังผู้ใช้" |

**ผลลัพธ์:** ลูกค้าทักมา → ได้ auto-reply ทันที 24 ชม. (keyword "ขอจดหมาย/จดหมาย" + Default) → เจ้าของเข้าไปคุยต่อ/ปิดการขายเองได้ในแชทเดียวกัน

## ที่ตรวจแล้วดีอยู่ (ไม่ต้องแก้)
- ข้อความทักทายเพื่อนใหม่: เปิด · มีลิงก์ ngernduangold.com/links + disclosure ครบ
- auto-reply 2 ตัวเปิดใช้งาน: `ขอจดหมาย-leadmag` (keyword 2 คำ, ตลอดเวลา) + `Default` (ตอบทุกข้อความ)
- /links hub: dual payment path ครบ (LINE พร้อมเพย์ + Gumroad บัตร) ทั้ง 199฿/59฿ · ebook v1.2 · disclosure ครบ

## ที่ยัง queue ไว้
- คำต้องห้าม "อนุมัติไว" ใน hero /links (build_site.py ~2640) → อยู่ใน CC-ORDER_internal-link-architecture PHASE 0
- Rich menu: ยังไม่ได้ตรวจ (แพ็กเกจฟรี — ถ้ายังไม่มี ให้พิจารณาสร้างเมนู 3 ปุ่ม: เครื่องคำนวณ / สั่งชุดจดหมาย / ปรึกษา)

## บทเรียนเชิงระบบ
Funnel มีจุดตายที่ **ปลายทาง** ได้ ทั้งที่ต้นทาง (คอนเทนต์/SEO/โพสต์) ทำงานปกติ — ควรตรวจ "ปลายทางรับสายได้จริงไหม" เป็นระยะ ไม่ใช่ดูแค่ traffic
