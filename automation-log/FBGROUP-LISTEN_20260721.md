# FBGROUP-LISTEN — 21 ก.ค. 2026 (Cowork · ~10:29)

## สรุปสั้น
- **Identity ที่ใช้:** ✅ เพจ เงินเดือนสมองทอง (ถูกต้อง)
- **กลุ่มที่เลือกวันนี้:** รวมพลคนอยากปิดหนี้ รวบหนี้ (id 2145569355698267)
- **จำนวนร่าง:** 0
- **สัญญาณ mod:** ไม่พบ (สมาชิกภาพยังปกติ "เข้าร่วมแล้ว")
- **ผลลัพธ์:** ⏸ ติด background-tab wall — อ่านฟีดไม่ครบ **ต้องให้เจ้าของคลิกแท็บ Facebook ขึ้นหน้าจอ แล้วรันซ้ำ**

---

## ด่าน 0 — Identity ✅ ผ่าน
- เปิด facebook.com → composer อ่านได้ว่า **"คุณคิดอะไรอยู่ เงินเดือนสมองทอง"**
- ในฟีดกลุ่มทุกโพสต์ขึ้น **"แสดงความคิดเห็นในชื่อ เงินเดือนสมองทอง"** → ยืนยันว่าถ้าคอมเมนต์จะออกในนามเพจถูกต้อง
- ไม่เจอ login wall

## การเลือกกลุ่ม (กฎหมุนเวียน)
grep ledger `channel=fb-group` → มีคอมเมนต์จริงแค่กลุ่มเดียว = **วิถีออมเงินมนุษย์เงินเดือน** (19 ก.ค.)
งาน 20 ก.ค. ร่างให้ "ปรึกษาหนี้บัตรเครดิตฯ" แต่**ยังไม่ได้โพสต์** (โพสต์เป้าหมายปิดคอมเมนต์ + ติด tab wall) → ยังไม่นับว่าคอมเมนต์แล้ว
→ วันนี้เลือก **รวมพลคนอยากปิดหนี้ รวบหนี้** — ยังไม่เคยคอมเมนต์ · intent สูงสุดตาม shortlist · ยังไม่เคยสแกน

## ด่าน 0.5 — แท็บ visible ❌ ไม่ผ่าน (ติด wall)
วัดค่าจริง (หลัง reload สะอาด ไม่ spoof):
- `document.visibilityState = "hidden"` · `document.hidden = true` · `document.hasFocus() = false`
- `document.body.innerText.length = 846` (< 2000 → เข้าเงื่อนไขหยุดตามกฎ)
- ฟีดเรนเดอร์แค่ 3 โพสต์ (skeleton ที่เหลือว่างเปล่า)

**พยายามปลดล็อก 2 วิธี (แบบมีขอบเขต ไม่ใช้คีย์ลัดต้องห้าม) — ล้มเหลวทั้งคู่:**
1. Spoof `visibilityState=visible` + dispatch visibilitychange + scroll 6 รอบ → bodyLen ไม่ขยับ (846 เท่าเดิม) เพราะ Chrome throttle ระดับเบราว์เซอร์ (rAF/network) ที่ JS หน้าเว็บ override ไม่ได้
2. resize_window 1400x900 + reload สด → ยังคง hidden/hasFocus=false, bodyLen 846 → หน้าต่างไม่ได้ย่อ แต่แท็บเราไม่ใช่แท็บ active ในหน้าต่าง
- **ไม่ใช้ SendKeys/Ctrl+9** ตามข้อห้ามใน SKILL ด่าน 0.5

## โพสต์ที่เรนเดอร์ได้ (แค่ 3 — ทั้งหมดเป็นโฆษณานายหน้า/บริการ จะข้ามอยู่แล้ว)
| จขกท. | เวลา | เนื้อหา | ตัดสิน |
|---|---|---|---|
| APA Real Estate | 3 นาที | คอนโดชลบุรีเงินผ่อน #รับนายหน้า + LINE | ❌ โฆษณานายหน้า |
| Hathaithip Donyouprai | 29 นาที | "ปิดหนี้ให้จริง ปิดก่อนยื่นกู้..." | ❌ โฆษณาบริการรับปิดหนี้ |
| Hathaithip Donyouprai | 1 ชม. | "ปิดหนี้ให้จริง ปิดก่อนยื่นกู้..." | ❌ โฆษณาบริการรับปิดหนี้ |

⚠️ **ห้ามสรุปว่ากลุ่มนี้ไม่มีโพสต์ที่ตอบได้** — ฟีดโหลดไม่ครบเพราะ tab wall โพสต์ขอความช่วยเหลือจริง (ถ้ามี) อาจอยู่ถัดลงไปที่ยังไม่เรนเดอร์

## วิธีปลดล็อก (ต้องให้เจ้าของทำ)
1. คลิกแท็บ Facebook ในเครื่องให้ขึ้นหน้าจอ (เป็นแท็บ active / `visibilityState=visible`)
2. บอก Cowork ให้รันงานนี้ซ้ำ → จะอ่านฟีด ~15 โพสต์ หาโพสต์ผ่านเกณฑ์ 4 ข้อ + เช็กคอมเมนต์เปิด แล้วส่งร่างให้เคาะ

## Ledger
❌ **ไม่ append** — ไม่มีคอมเมนต์เกิดขึ้น (append เฉพาะหลังเจ้าของอนุมัติและโพสต์แล้วเท่านั้น)

## หมายเหตุเชิงยุทธศาสตร์
สอดคล้องกับข้อค้นพบ 20 ก.ค. — กลุ่มหนี้บน FB นายหน้า/รับปิดหนี้ครองฟีด (3 โพสต์แรกที่เห็นวันนี้เป็นโฆษณาทั้งหมด) แต่**ยังตัดสินกลุ่มนี้เต็มไม่ได้**เพราะติด tab wall · ถ้ารันซ้ำแล้วเจอแต่โฆษณานายหน้าอีก อาจยืนยันว่ากลุ่มนี้คุณภาพต่ำเหมือนกลุ่มหนี้อื่น (ข้อมูลประกอบ gate 27 ก.ค.)
