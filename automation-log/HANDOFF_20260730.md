# ส่งไม้ต่อ — 30 ก.ค. 2026 (ปิดวัน)

> อ่านไฟล์นี้ก่อนเริ่มงานพรุ่งนี้ · ทุกบรรทัดยืนยันจากการรันจริง ไม่ใช่ความจำ

---

## สถานะระบบ ณ ปิดวัน

```
commit ล่าสุด   bf1a137   (push แล้ว · origin..HEAD ว่าง)
preflight       6/6 PASS
post_guard      exit 2 = สะท้อนความจริง (TikTok/Threads วันนี้ยังไม่ได้ลง)
คิวคอนเทนต์     7 วัน ถึง 5 ส.ค. · 3 ไฟล์ตรงกัน
เว็บ production attribution fix LIVE (ยืนยันจากเนื้อหาหน้าเว็บ)
scheduled task  เปิดกลับ 12/16 + preflight ฝังใน dispatcher แล้ว
```

## สิ่งที่เปลี่ยนวันนี้ (5 commit)
| commit | สาระ |
|---|---|
| `2f1e064` | แก้ attribution lifestyle · ต่อคิว batch3 · เพิ่ม runway_guard + preflight |
| `e85fa55` | ฝัง preflight ใน dispatcher (รันแม้ task ปิดหมด) |
| `7203522` | post_guard เลิกคืน UNKNOWN ถาวร · track yt_upload_log |
| `1d4201e` | บันทึก ground truth การทดสอบ Threads |
| `bf1a137` | บันทึกข้อสรุป TikTok web + ขอบเขต permission gate |

## บั๊กที่ปิดวันนี้ (ทั้งหมดเป็นชนิด "เงียบ")
1. **ท่อขาด** — batch3 อยู่ใน manifest แต่ไม่อยู่ใน schedule/content_map → 4 ช่องเงียบ 4 วัน
2. **attribution leak** — hero CTA หน้า lifestyle ยิง `channel=website` มา 11 วัน
3. **`\n` ตัวหนังสือ** ใน 35 แคปชัน
4. **ลิงก์ YouTube ชี้หน้า noindex** 4 จาก 7 คลิป
5. **post_guard นับแถว `type=failure` เป็นหลักฐานว่าโพสต์สำเร็จ** ← อันตรายสุด
6. **"YT token ตาย"** ที่จริงเป็นแค่ scope อ่านไม่ได้ → ทำให้อัปมือโดยไม่จำเป็นหลายวัน
7. **ตั้งเวลาเผยแพร่ย้อนอดีต** ใน uploader
8. **วันที่ฮาร์ดโค้ด** ใน uploader (batch3 อัปด้วยสคริปต์ไม่ได้เลย)

## เครื่องมือใหม่
- `tools/preflight.py` — 6 ด่าน คำสั่งเดียวพิสูจน์ทั้งระบบ (`--full` เพิ่ม build gate · `--json`)
- `tools/runway_guard.py` — เตือนก่อนคิวหมด/ก่อนไฟล์ drift
- `tools/test_post_guard_status.py` — regression 13 เคส (CC เขียน)
- ทั้งสามผ่านการ **ทดสอบย้อนศร** แล้ว (จงใจทำของพัง ดูว่ามันร้องไหม)

---

## ⏳ ค้างอยู่ — ต้องใช้มือเจ้าของ

| งาน | ทำไมทำแทนไม่ได้ | ของพร้อมแล้ว |
|---|---|---|
| วางคำตอบ Pantip 44179525 | permission gate บล็อกการโพสต์สาธารณะแทนเจ้าของ (ลอง 2 ครั้ง) | ข้อความอยู่ในกล่องโค้ด `#ngernduangold-action` · บัญชี 9373300 ยืนยันแล้ว |
| อัป TikTok + FB Reel (b3-01) | เส้นทางเดียวกัน | `reels/2026-07-27_b3-01.mp4` + แคปชันในการ์ด |
| เช็กสถานะบัญชี TikTok | เว็บไม่เรนเดอร์หน้านี้ (ทดสอบ 3 ทาง) | ต้องเปิดแอปมือถือ |

⚠️ **คลิปบอร์ดถูกเขียนทับแล้ว** — อย่าบอกให้ Ctrl+V ให้ก๊อปจาก Slack แทน

## 🌙 ทำงานเองคืนนี้
- **03:00** `threads-video-idle-retry` — ทดสอบตัวแปรสุดท้ายของ Threads (เครื่องว่างจริง) รายงานเข้า `#ngernduangold-ops` เอง
- **08:00** การ์ดงาน — ตอนนี้เช็ก **"ค้างจากเมื่อวาน"** ให้ด้วยแล้ว (TikTok/FB ของ 30 ก.ค. จะไม่หายไปเฉยๆ)
- **19:25** post-guard + preflight → `#ngernduangold-ops`

## 📌 รอตัดสิน
1. **gate batch3 → 6 ส.ค. 10:00** (task ตั้งไว้แล้ว) — จะบอกว่าผลิต batch4 เต็มหรือลดเหลือ 3/สัปดาห์
2. **เกณฑ์ TikTok 10 ส.ค.** — เกณฑ์เดิม 21 คลิปเป็นไปไม่ได้ · gate จะเสนอเกณฑ์ใหม่ให้เคาะ · **ยังต้องรู้สถานะบัญชีก่อน**
3. **Pantip เฟส 2** — เฟส 1 หมดวันนี้ · ยังไม่ได้ตัดสินว่าต่อแบบไหน

---
*ปิดวัน 30 ก.ค. 2026 23:0x · ผู้เขียน: Cowork · ตรวจงาน CC ด้วยการรันซ้ำเองทุกข้อ (MERGE GATE)*
