# FBGROUP-LISTEN — 9 ส.ค. 2026 (Cowork · scheduled run)

## สรุปสั้น
- **Identity:** ✅ **เพจ เงินเดือนสมองทอง** (`คุณคิดอะไรอยู่ เงินเดือนสมองทอง` · หน้าโพสต์ขึ้น `แสดงความคิดเห็นในชื่อ เงินเดือนสมองทอง`) · ไม่มี login wall · ไม่ต้องสลับตัวตน
- **กลุ่มวันนี้:** **ปิดหนี้ อยากมีบ้าน บูโรฯ** (`groups/2413835685463430`) — **เป้าหมายเดิมจาก 8 ส.ค. ยังมีชีวิต จึงเสนอซ้ำ ไม่ร่างใหม่ทับ**
- **จำนวนร่าง:** **1 ร่าง (รอบ 2 · เป้าหมายเดิม · ตัดสั้นลง 36%)** — ไม่ได้สำรวจหาเป้าหมายใหม่ ตามขั้นตอนข้อ 2 ของ SKILL
- **soft-CTA:** ✅ ใส่ (โครงเดิม: ขอ 3 ข้อ · ไม่ขอ PII · ฟรี) + ประโยคแยกตัวจากนายหน้า
- **สัญญาณ mod:** ❌ ไม่พบ — คอมเมนต์ 26 ก.ค. **ยังอยู่ + ยังอันดับ 1 (วันที่ 14)**
- **ไม่ได้คอมเมนต์/โพสต์เอง · ไม่กรอกรหัส/2FA · ไม่กดปุ่มใด ๆ**

---

## 🔴 การตัดสินใจหลักวันนี้: **ไม่ร่างใหม่ — เสนอเป้าหมายเดิมรอบ 2 พร้อมตัดความยาว**

`grep post-ledger.jsonl | channel=fb-group` → คอมเมนต์จริงล่าสุดยังเป็น **26 ก.ค.** = **ค้าง 14 วัน** → ร่าง 8 ส.ค. **ไม่เคยถูกโพสต์**

ตรวจสดโพสต์เป้าหมายเดิม `groups/2413835685463430/posts/3385486484965007`:

| เช็ก | 8 ส.ค. | **9 ส.ค.** | อ่านผล |
|---|---|---|---|
| โพสต์ยังอยู่ | ✅ | ✅ | — |
| คอมเมนต์เปิด | ✅ | ✅ (`แสดงความคิดเห็นในชื่อ เงินเดือนสมองทอง`) | — |
| อายุ | 1 วัน | **2 วัน** | ยังอยู่ในกรอบ |
| รีแอค | 40 | **40 (นิ่ง)** | ชะลอ |
| คอมเมนต์ | 64 | **65 (+1)** | **ยังไหลอยู่ ไม่ตาย** |
| มีคำตอบดี ๆ ครอบหรือยัง | ❌ | ❌ **ยังไม่มี** | ช่องว่างยังเปิด |

> เทียบกับเคสที่ปลดระวางไปแล้ว (worldprothailand): คอมเมนต์ **นิ่งสนิท 2 วันเต็ม + รีแอคนิ่ง** = ตายจริง
> เคสนี้ **ยังได้คอมเมนต์ใหม่ +1 ใน 24 ชม.** = ยังมีคนไหลเข้ามาอ่าน → **ยังไม่เข้าเงื่อนไขปลดระวาง** (เกณฑ์ที่ตั้งไว้เอง: เสนอรอบ 3 + อายุ ≥4 วัน · วันนี้ = รอบ 2 + อายุ 2 วัน)

**ภูมิทัศน์คอมเมนต์ (ตรวจซ้ำวันนี้ ไม่เปลี่ยน):** 10 บล็อกที่อ่านได้ — นายหน้า "ทักมา" 6 ราย (Nus Manuschanok +เบอร์+LINE · นิ คุณนายโฮม +LINE · AngelStar.CH · GlowingTurtle4686 +ลิงก์ · Punpun · Kimhong/Sukanda) · ตอบห้วนไม่มีเหตุผล 2 ราย (`ไม่ถึงครับ` / `ไม่ถึงคะ`) · **มีเนื้อจริงคนเดียวยังคงเป็น Preecha Gapant** ("แนะนำไม่เกิน 2 ล้าน" — ถูกทางแต่ไม่บอกวิธีคิด)

---

## 📏 ข้อค้นพบใหม่วันนี้ — **วัดความยาวของคอมเมนต์ที่ "พิสูจน์แล้วว่าเวิร์ก"**

เนื่องจากคอขวดคือการส่งมอบ ไม่ใช่คุณภาพ จึงวัดของจริง: คอมเมนต์ 26 ก.ค. ที่**ยืนอันดับ 1 มา 14 วัน**ยาวเท่าไร

| ชิ้น | ความยาว | ผลจริง |
|---|---|---|
| **คอมเมนต์ 26 ก.ค. (ที่เวิร์ก)** | **2,245 ตัวอักษร** | ✅ อันดับ 1 · 14 วัน · ไม่ถูกลบ |
| ร่าง 8 ส.ค. (ไม่ถูกโพสต์) | **4,523 ตัวอักษร** | ❌ ค้าง 1 วัน |
| **ร่างวันนี้ (ตัดแล้ว)** | **2,878 ตัวอักษร** | รอเจ้าของ |

> **ข้อสรุป:** ร่าง 8 ส.ค. ยาว **2.0 เท่า**ของฟอร์แมตที่พิสูจน์แล้วว่าใช้ได้ในกลุ่มนี้ — เป็นสมมติฐานที่จับต้องได้ว่าทำไมถึงไม่ถูกใช้ (เจ้าของต้องอ่านทวนกำแพงตัวอักษรก่อนวาง)
> จึง**ตัดลง 36%** เหลือ 2,878 (1.28 เท่าของฟอร์แมตที่เวิร์ก) โดย**คงแกนเดิมทุกข้อ**: มุม MOU ที่ไม่มีใครพูด · วิธีที่ธนาคารคิดวงเงิน · เครื่องมือทางการ · กับดักเงินทอน · เตือนภัย · 1213 · soft-CTA
> สิ่งที่ตัดออก: ข้อ "เงินก้อนที่ไม่ใช่ค่างวด" (ค่าโอน/จดจำนอง/ตกแต่ง) และคำอธิบายว่าทำไม 1443 ไม่ใช่เคสนี้ — ทั้งสองข้อดี แต่ไม่ใช่แกนที่ตอบคำถาม OP โดยตรง · **ฉบับเต็มยังอยู่ในไฟล์ 8 ส.ค. ถ้าอยากใช้แทน**

---

## ✅ สัญญาณ mod — สะอาด (วันที่ 14)
| หลักฐาน (ตรวจสดวันนี้) | ผล |
|---|---|
| คอมเมนต์ 26 ก.ค. (`posts/3372747312905591`) | ✅ **ยังอยู่ครบ · ยังเป็นรายการแรกใต้ `เกี่ยวข้องมากที่สุด`** · ตรวจเจอทั้ง `อ่านแล้วเข้าใจสถานการณ์` และ `1443` · ยาว 2,245 ตัวอักษร ไม่ถูกตัด |
| คำ `ละเมิด/ถูกลบ/ถูกซ่อน/ระงับ/คำเตือน/ไม่เป็นไปตามมาตรฐาน` | ✅ 0 hit ทุกหน้าที่เปิด |

---

## ⏳ กลุ่ม "ปลดหนี้ ฉบับมนุษย์เงินเดือน" — **ยังไม่อนุมัติ (วันที่ 21)**
ตรวจสด `groups/198859094628050`: พบ `รอการอนุมัติ` · **ไม่มีปุ่ม `เข้าร่วมกลุ่ม`** (= คำขอยังค้างจริง ไม่ได้ถูกปฏิเสธ) · `isMember=false` → ยังคอมเมนต์ไม่ได้
เตือนไว้: วันไหนอนุมัติ = **ห้าม soft-CTA เด็ดขาด** (กฎกลุ่มห้ามขาย ลบทันที)

---

## ⚙️ บันทึกเทคนิค
- `document.visibilityState = 'hidden'` ตลอดรอบ · **ไม่เข้าเงื่อนไขหยุดของด่าน 0.5** — หน้าโพสต์เดี่ยว `[role=article]` = 28 (> 1), `innerText` = 3,871 (> 2,000)
- ยืนยันซ้ำเป็นวันที่ 2: **หน้าโพสต์เดี่ยวเรนเดอร์ครบแม้แท็บอยู่เบื้องหลัง** → เปิดด้วย URL ตรงคือเส้นทางที่เชื่อถือได้ที่สุด ไม่ต้องพึ่งหน้าฟีดกลุ่ม
- `body.innerText` ถูก throttle เหลือแต่ sidebar (2,423 ตัวอักษร ซ้ำทุกหน้า) → **ต้องอ่าน `[role=article]` ที่ยาวที่สุดเสมอ**
- เทคนิคใหม่: แยกบล็อกคอมเมนต์ด้วยการ split ที่ trailer `ถูกใจ\nตอบกลับ\nแชร์` → ได้รายการคอมเมนต์เป็นก้อน ๆ อ่านง่ายกว่าการ regex ทีละชื่อ

---

## 📄 ร่างที่เสนอ (รอบ 2 · ตัดสั้น · เป้าหมายเดิม)

```
อ่านแล้วเข้าใจคำถามครับ รายได้ 29,000 มีสิทธิ์ MOU กับ ธอส. แล้วอยากรู้ว่ากู้บ้าน 4 ล้านไหวไหม สังเกตว่าคอมเมนต์แบ่งเป็นสองฝั่งที่ตอบตรงข้ามกันเลย แต่ไม่มีใครบอกวิธีคิดสักคน ผมขอเล่าวิธีที่ธนาคารใช้ตัดสินจริง เพื่อให้คุณคำนวณเองได้ ไม่ต้องเดาตามคอมเมนต์ใคร

หนึ่ง MOU ช่วยเรื่องเงื่อนไข เช่น อัตราดอกเบี้ยที่ดีกว่าลูกค้าทั่วไป ค่าธรรมเนียมบางรายการ หรือขั้นตอนที่สะดวกขึ้น แต่ MOU ไม่ได้เพิ่มรายได้ของคุณ จึงไม่ได้ยกเพดานความสามารถผ่อนขึ้นแบบก้าวกระโดด หลายคนเข้าใจผิดข้อนี้แล้วไปผิดหวังตอนยื่นจริง

สอง ธนาคารไม่ได้เอาราคาบ้านเป็นตัวตั้ง เขาเริ่มจากรายได้ที่พิสูจน์ได้ แล้วหักภาระผ่อนที่มีอยู่ออกให้หมด ทั้งค่างวดรถ บัตรเครดิต สินเชื่อส่วนบุคคล กยศ. หรือที่ไปค้ำประกันให้ใครไว้ ส่วนที่เหลือคือกำลังผ่อนต่อเดือนของคุณ แล้วยังมีเพดานอีกชั้นว่าภาระผ่อนรวมต้องไม่เกินสัดส่วนหนึ่งของรายได้ สุดท้ายเขาถึงแปลงค่างวดกลับมาเป็นวงเงินกู้ โดยระยะเวลาผ่อนถูกจำกัดด้วยอายุคุณอีกที แปลว่าตัวแปรที่คุณคุมได้จริงมีสองอย่าง คือหนี้เดิมที่ปิดได้ก่อนยื่น กับระยะเวลาผ่อนตามอายุ ไม่ใช่การไปหาคนที่บอกว่าจัดให้ได้

สาม ก่อนเชื่อคอมเมนต์ใครทั้งนั้น ธอส. มีเครื่องมือคำนวณวงเงินกู้เบื้องต้นให้กรอกเองบนเว็บไซต์และแอปของธนาคาร ใช้ฟรี ไม่ต้องผ่านใคร ใส่รายได้ ภาระผ่อน และอายุลงไป จะเห็นตัวเลขคร่าว ๆ จากเจ้าของเกณฑ์ตัวจริงทันที แล้วโทรคอลเซ็นเตอร์หรือเดินเข้าสาขา ธอส. เอง แจ้งว่าจะใช้สิทธิ์ MOU แล้วขอให้ประเมินวงเงินเบื้องต้นให้ อันนี้คือคำตอบเดียวที่มีน้ำหนักจริง เพราะมาจากคนที่เป็นคนอนุมัติ และควรขอดูรายงานเครดิตบูโรของตัวเองก่อนยื่นด้วย ประวัติค้างเก่าที่เราลืมไปแล้วมักโผล่ตอนธนาคารเรียกดู รู้ตอนนี้ดีกว่าไปรู้ตอนวางเงินจองไปแล้ว

สี่ กับดักที่อยู่ในคอมเมนต์ของโพสต์นี้เอง คือข้อเสนอปิดหนี้ให้ก่อนยื่นกู้ กับการจัดให้กู้ผ่านแล้วมีเงินทอน เงินที่เขาออกปิดหนี้ให้ไม่ใช่เงินฟรี มันคือหนี้ก้อนใหม่ที่เงื่อนไขมักแพงกว่าเดิม และมักผูกว่าต้องซื้อบ้านจากคนที่จัดให้เท่านั้น ส่วนเงินทอน เนื้อแท้คือการทำให้ยอดกู้สูงกว่าราคาซื้อขายจริงแล้วเอาส่วนต่างออกมาเป็นเงินสด จุดที่คนชวนมักไม่บอกคือ คนที่ชื่ออยู่ในสัญญากู้และรับความเสี่ยงทั้งหมดถ้าเรื่องแดงขึ้นมา คือคุณ ไม่ใช่คนที่จัดให้

เรื่องความปลอดภัย อย่าโอนค่าดำเนินการหรือค่าจัดไฟแนนซ์ล่วงหน้าให้ใครที่ทักมาในโพสต์ อย่าส่งรูปบัตรประชาชน สลิปเงินเดือน หรือรหัส OTP ให้คนแปลกหน้า และอย่ายอมให้ใครใช้ชื่อคุณไปกู้แทน ถ้าเจอคนเร่งให้ตัดสินใจ หรือเสนอเงื่อนไขที่ฟังแล้วดีเกินจริง โทรถามศูนย์คุ้มครองผู้ใช้บริการทางการเงิน ธนาคารแห่งประเทศไทย ที่ 1213 ก่อนเซ็นอะไรทั้งนั้น

ส่วนคอมเมนต์ที่แนะนำให้ดูงบราวสองล้าน อยู่ในทิศทางที่สมเหตุสมผลกว่าสี่ล้านครับ ถ้าอยากขยับเพดานจริง ๆ ทางตรงมีแค่สองทาง คือกู้ร่วมกับคนที่มีรายได้ ซึ่งรวมรายได้กันได้แต่ผูกกันทั้งสองฝ่าย และจะไปนับรวมในประวัติของเขาเวลาเขาจะกู้อะไรของตัวเอง กับลดงบบ้านลงมาให้ตรงกำลังจริงก่อน แล้วค่อยขยับเมื่อหนี้เดิมหมด

ถ้าไม่แน่ใจว่าจะถามธนาคารยังไงให้ได้ตัวเลขจริงตั้งแต่ครั้งเดียว ทักมาบอกแค่สามอย่าง คือรายได้ต่อเดือน ภาระผ่อนที่มีอยู่รวมเดือนละเท่าไร และอายุเท่าไร เดี๋ยวผมช่วยเรียบเรียงคำถามให้ฟรีครับ ไม่ต้องบอกชื่อ เบอร์ หรือเลขบัญชี ผมไม่ได้ขายบ้าน ไม่ได้จัดไฟแนนซ์ และไม่ได้รับปิดหนี้นะครับ
```

> ✅ ไร้ลิงก์ · ไร้แบรนด์ · ไร้ราคา · ไร้ตัวเลขดอกเบี้ย · ไร้สัดส่วน DSR · ไร้การันตี (ตรวจอัตโนมัติ: 0 flag)
> ✅ เตือนภัยครบ (เงินทอน = ยอดกู้สูงกว่าราคาจริง · ห้ามโอนล่วงหน้า · ห้ามส่ง OTP/บัตรประชาชน · ห้ามให้ใครใช้ชื่อกู้แทน)
> ✅ อ้าง 1213 (ศคง. ธปท.) — ไม่อ้าง 1443/1359 เพราะเคสนี้ยังไม่เป็นหนี้เสียและไม่ใช่หนี้นอกระบบ
> ✅ มีประโยคแยกตัวจากนายหน้า (กระทู้นายหน้าเต็ม → เข้าเงื่อนไข SKILL)
> ✅ ทดสอบ: ตัด soft-CTA ออกแล้วยังมีค่าเต็ม (ข้อ 1–4 + เตือนภัย ยืนได้เอง)

---

## ⏳ สถานะกลุ่ม
| กลุ่ม | สถานะ |
|---|---|
| **ปิดหนี้ อยากมีบ้าน บูโรฯ** | 🎯 **เลือกวันนี้ (รอบ 2)** · โพสต์ 2 วัน คอมเมนต์เปิด ยังได้คอมเมนต์ใหม่ |
| ปิดหนี้ บัตรเครดิต นอกระบบ รวมหนี้ฯ | ⏸ พัก — ร่างเดิมปลดระวาง 8 ส.ค. |
| วิถีออมเงินมนุษย์เงินเดือน | 🔄 รอบถัดไป (ใช้คำค้น `ปรึกษา` / `ขอคำแนะนำ`) |
| รวมพลคนอยากปิดหนี้ รวบหนี้ | 🔴 โฆษณานายหน้า ~100% |
| สิทธิประโยชน์ & โปรบัตรเครดิต | 🟡 สำรอง (คนละ intent) |
| ปลดหนี้ ฉบับมนุษย์เงินเดือน | ⏸ **รออนุมัติ วันที่ 21** · ถ้าอนุมัติ = ห้าม soft-CTA |
| ปรึกษาหนี้บัตรเครดิตและสินเชื่อ | ⛔ ถอดถาวร 26 ก.ค. (ปิดคอมเมนต์ทั้งกลุ่ม) |

## ⚠️ เส้นตายที่ตั้งไว้เอง
ถ้าร่างนี้ยังไม่ถูกโพสต์ภายใน **10 ส.ค.** (= เสนอรอบ 3 + โพสต์อายุ 3 วัน) และคอมเมนต์/รีแอคนิ่งสนิท 2 วันเต็ม → **ปลดระวาง** แล้วหาเป้าหมายใหม่ ตามเกณฑ์เดียวกับ Terry9024 และ worldprothailand

## ขั้นตอนหลังเจ้าของเคาะ
1. เปิด URL → ยืนยันช่องขึ้น **"แสดงความคิดเห็นในชื่อ เงินเดือนสมองทอง"** ก่อนวาง
2. วาง **1 โพสต์เท่านั้น** วันนี้ (เพดาน ≤1 กลุ่ม/วัน)
3. append ledger: `channel=fb-group` · `group=ปิดหนี้ อยากมีบ้าน บูโรฯ` · `post=https://www.facebook.com/groups/2413835685463430/posts/3385486484965007/` · `policy=no-link-no-brand-no-price`
