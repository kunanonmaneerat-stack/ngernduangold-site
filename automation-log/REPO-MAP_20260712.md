# REPO-MAP — ngernduangold-site · WO-1 discovery · 12 ก.ค. 2026 (CC)

> ทำโดย CC ที่ถือ context เต็มของงาน 9–11 ก.ค. — ทุก path ตรวจกับไฟล์จริง ไม่เดา

## 1) Generator / Build
- **Generator เดียว: `build_site.py`** (~2,800 บรรทัด, Python stdlib ล้วน, มีภาษาไทยในไฟล์) → เขียน output ลง **`site/`** (gitignored)
- Local build: `SITE_BASE=https://ngernduangold.com SITE_GA=G-17PPE0M1B8 python3 build_site.py`
- Netlify (`netlify.toml`): command = `python3 build_site.py && python3 tools/postdeploy_smoke.py --src site` · publish = `site` · Python 3.11
- **⚠️ ignore rule ปัจจุบัน exclude แค่ 3 dir**: `automation-log` · `pipeline` · `tiktok-pipeline`
  - **GAP vs guardrail 3: `social-autopost/` และ `.system_control/` ยังไม่ถูก exclude** → commit ที่แตะ 2 โฟลเดอร์นี้ **trigger build จริง** (เกิดแล้วกับ commit social-autopost 11 ก.ค. — เปลือง build minutes)
  - หมายเหตุ: แท็ก `[skip-build]` ในข้อความ commit **ไม่มีผลใด ๆ** — ignore rule เป็น path-based เท่านั้น
  - ข้อเสนอ: เพิ่ม `':(exclude)social-autopost' ':(exclude).system_control' ':(exclude)cc-inbox'` — แต่การแก้ netlify.toml เอง trigger 1 build → **รวมแก้ไปกับ commit หน้าเว็บครั้งถัดไป** (เช่น WO-5) ประหยัดสุด
- Gate อื่นก่อน push (convention เดิม): `check_affiliate_links.py` (root) 17/17 · `pipeline/link_check.py` 0 broken · `pipeline/comply_gate.py` · smoke 67/67 หน้า

## 2) Content — บทความ 55 ตัว (สำคัญ: ไม่มี content folder!)
- **ไม่มี markdown/front-matter** — บทความทั้ง 55 เป็น **f-string ฝังใน `build_site.py`**:
  - รูปแบบ: `slugN="<slug>.html"` + `bodyN=f"""<h1>..."""` + **`ART.append((slug,title,desc,body,faqs,camp))`** — นับจริง 55 ครั้ง
  - writer loop (~L2340): ประกอบ head/nav/top_offer/calc_cta/kept_next/letter_cta/ebook_banner/QUIZ_CTA/FOOTER อัตโนมัติรอบ body
- **เพิ่มบทความใหม่** = แก้ `build_site.py`: เพิ่ม slug/body + `ART.append(...)` + (ถ้าเด่น) sitemap priority ใน `urls` list — **ต้องแก้ผ่าน python I/O anchored-replace เท่านั้น** (บทเรียน mojibake: Edit/Write tool ตรง ๆ กับไฟล์ไทยเคยตัดกลางไฟล์ · heredoc mangle `\\`)
- **เพิ่มหน้า tool/landing แบบ standalone** = สร้าง `<slug>.html` ที่ root + ลงทะเบียนใน `_TOOL_PAGES` (build_site.py ~L50: copy + head-inject canonical/OG/twitter/GA + fail-closed guard) + sitemap `urls` + `_redirects` 200-rewrite — มี precedent 6 หน้าแล้ว
- URL convention: **extensionless canonical** ทั้งไซต์ (post-build normalizer + per-page 301 ใน `_redirects` — generate อัตโนมัติท้าย build_site.py)

## 3) Winner pages — path จริง
| หน้า | สร้างจาก | แก้ที่ |
|---|---|---|
| `/kept-savings-2026` | ART entry ใน build_site.py (`kept-savings-2026.html`) | build_site.py (python I/O) |
| `/debt-calculator` | **root `debt-calculator.html`** (standalone + head-inject) | ไฟล์ root โดยตรง (python I/O) |
| `/links` | เขียนท้าย build_site.py (hub + buy zone id="buy") | build_site.py |
| `/title-loan-2026` | ART entry | build_site.py |
| `/debt-consolidation-2026` | ART entry (มี cmp_widget + Kashjoy row) | build_site.py |
- หน้า interactive ที่ **LIVE แล้ว** (11 ก.ค.): `/debt-health-check` (quiz หนี้ A–F) · `/refinance-savings-calculator` · `/debt-freedom-clock` · `/debt-letter-kit` (ขาย 199฿) · `/workshop-hr` — ทั้งหมด standalone ผ่าน `_TOOL_PAGES`

## 4) Affiliate — ลิงก์อยู่ที่ไหน
- ค่าคงที่ใน build_site.py (~L898): KRUNGSRI/KEPT/SRISAWAD/NGERNTURBO (`atth.me/00eabp002a0x`)/KASHJOY (`atth.me/008izv002a0x`)/HappyCash ฯลฯ + dict `_GO` → generate `/go/*` 301 ใน `_redirects` · AXA PA = `atth.me/go/PhAKgrKX` (ยืนยันแคมเปญแล้ว 11 ก.ค.)
- `recommend_map.json` (root) = fit/why/watch ต่อ provider · **17 ลิงก์** เช็ค HTTP ด้วย `check_affiliate_links.py` (root ไม่ใช่ tools/)
- rel="sponsored"+disclosure ถูก assert โดย `tools/postdeploy_smoke.py` (CANON set + FTC order check) — **WO-4 มีฐานนี้ต่อยอดได้ ไม่ต้องเขียนจากศูนย์**

## 5) social-autopost / control-plane ที่มีอยู่แล้ว (สถานะ 12 ก.ค. 00:30)
- `social-autopost/content_map.json` — **16 entries (11–26 ก.ค.)** schema: `{clipFile, slug, igCaption, tiktokCaption, affiliate, clipPending?}` · แคปชัน comply-verified 16/16 · **20–26 clipFile = placeholder `batch2/` รอ render**
- `feed_content_map.json` — FB 7 โพสต์ (12–18) · publishers 3 ตัว (`automation/ig_publish.py`, `social-autopost/publish_fb.py`, `publish_tiktok.py`) + workflows `ig-reels` 20:00TH / `fb-feed` 15:00TH / `ig-token-check` — ทุกตัว soft-skip+alert จนกว่า secrets มา
- TikTok จริง ณ ตอนนี้: **11 ก.ค. โพสต์ live แล้ว + 12 ก.ค. ตั้งเวลา 19:00 แล้ว** (owner ทำผ่าน Studio native scheduler — เส้นทางที่ใช้ได้จริง) + ชีท 13–19 ใน cowork-inbox · dedup บันทึกใน `social-autopost/logs/` (gitignored)
- `REACH-CONTENT-STANDARD.md` + `STANDING-RULE_autopost.md` (root, ยัง untracked — Cowork วางไว้) — อ่านแล้ว ใช้เป็นกติกาแคปชัน/ความถี่

## 6) ⚠️ OVERLAP FLAGS — จุดที่ WO-2..5 ชนกับของที่มี/live แล้ว (ต้องเคลียร์ก่อนทำ)
1. **WO-5 ชนหนักสุด**: `/quiz` **มีอยู่แล้ว** = quiz ประกัน (insurance) · และ **quiz สุขภาพหนี้เกรด A–F ก็ LIVE แล้ว**ที่ `/debt-health-check` (7 คำถาม รวม DTI · routing A/B→kept, C/D→calc, E/F→consolidation+SAM · **การ์ดแชร์ PNG ทำเสร็จแล้ว** ไม่ใช่ stub · disclosure ครบ) — ทำตาม WO-5 ตรง ๆ = งานซ้ำ + slug ชน
   → ข้อเสนอ: **enhance `/debt-health-check`** ด้วย delta ที่ WO-5 มีเพิ่ม: sticky CTA ล่างจอ + ปุ่ม "ชุดเครื่องมือ 199฿" + ปุ่ม LINE OA + (option) เพิ่ม routing E/F ไป /links
2. **WO-3**: GSC **มี API auth อยู่แล้ว** (`pipeline/gsc_pull.py`, ga4-token webmasters scope) ไม่ต้องพึ่ง CSV export — ทำได้ทั้งสองโหมด (API หลัก, `data/gsc/*.csv` เป็น fallback ตามสเปก) · ความจริงจากดาต้า 10–11 ก.ค.: property มี **~7 query rows, ตำแหน่ง 29–70** (ยังไม่มี pos 8–20) — sprint ควร map จาก "บทความ→หน้าเงิน" เป็นหลัก ไม่ใช่รอ query data สุก
3. **WO-4**: มี `check_affiliate_links.py` (HTTP 17/17) + smoke asserts แล้ว — ของใหม่ที่คุ้มทำ = ตารางรายหน้า (หน้า|ลิงก์|rel|disclosure|HTTP) ตามสเปก → เขียนเป็น wrapper ต่อยอด
4. **WO-2**: ไม่ชน — migrate จาก content_map 16 entries ได้เลย (captions fb มีใน feed_content_map แยก; youtube/threads ยังว่าง)

## 7) คำตอบเกณฑ์รับงาน WO-1
- generator/содержание/output/netlify: ครบตาม §1–2 · winner paths: §3 · affiliate: §4 · front-matter: **ไม่มี** (inline f-string — ระบุวิธีแก้ที่ถูกใน §2)
- เพิ่มบทความ/หน้าใหม่ต้องแก้: `build_site.py` (ART หรือ `_TOOL_PAGES`) — ห้ามใช้ editor ตรงกับไฟล์ไทย ใช้ python I/O + assert + UFFFD check + git blob verify
