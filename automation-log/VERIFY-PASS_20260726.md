# ตรวจย้อนงานทั้งหมด (verification pass) — 26 ก.ค. 2026 ~02:00
ตรวจซ้ำทุกอย่างที่เปลี่ยนในรอบ consult (คืน 25 ก.ค. ต่อเนื่องถึงเช้ามืด 26 ก.ค.)
เป้าหมาย: ไม่ใช่ยืนยันว่า "ทำแล้ว" แต่หา **สิ่งที่ยังผิดอยู่**

---

## 🔴 พบเพิ่ม 3 จุด (ที่การตรวจรอบก่อนมองไม่เห็น) — แก้แล้วทั้งหมด

การตรวจรอบก่อนดูว่า task "สอดคล้องกับกลยุทธ์ไหม" แต่ไม่ได้ดูว่า **โค้ดมีวันหมดอายุซ่อนอยู่ไหม**
พอเช็กเวลาจริงถึงเห็นว่าตอนนี้ข้ามเที่ยงคืนเป็น **26 ก.ค.** แล้ว และ `tools/post_guard.py` hardcode ชุดวันที่ที่ **หมดอายุพรุ่งนี้พอดี**

### 1. `FB_TOKEN_BLOCKED_DATES` = 21–26 ก.ค. เท่านั้น → ตายเงียบวันที่ 27
- **อาการที่จะเกิด:** ตั้งแต่ 27 ก.ค. เงื่อนไข FB ไม่เข้าเลย → ตกไป `manifest_or_unknown` → รายงาน `UNKNOWN` ทุกวันแทนที่จะบอกว่าเป็น manual by design
- **ต้นตอเชิงแนวคิด:** FB manual ผ่าน Business Suite เป็น **การตัดสินใจถาวร** (Meta token ยกเลิก 18 ก.ค.) แต่ถูกเขียนเป็น "หน้าต่างชั่วคราว 6 วัน"
- **แก้:** `FB_MANUAL_FROM = date(2026, 7, 21)` + เช็ก `target >= FB_MANUAL_FROM` (เปิดปลาย ไม่มีวันหมดอายุ)
- **ทดสอบ:** 27 ก.ค. → `MANUAL-ONLY` ✓ · 5 ส.ค. → `MANUAL-ONLY` ✓ · 20 ส.ค. → `MANUAL-ONLY` ✓

### 2. `tiktok_schedule_note` ผูกกับชุดวันที่ 23–26 ก.ค.
- **อาการที่จะเกิด:** ตั้งแต่ 27 ก.ค. จะพิมพ์ `"No 23-26 Jul TikTok manual scheduling date is inside the current 10-day window"` **ตลอดไป** — ข้อความที่ไม่มีวันเป็นจริงอีก และไม่เตือนคลิปที่ต้องตั้งจริง
- **แก้:** derive จาก manifest แทน hardcode — เอาวันที่ที่มีคลิปวางแผนไว้ในช่วง 10 วันข้างหน้า · ถ้าไม่มีเลย ให้บอกว่า manifest จบวันไหนและต้องผลิต batch ถัดไป
- **ทดสอบ 3 เคส:**
  | เคส | ผล |
  |---|---|
  | อยู่ในช่วง manifest | `window is open for: 2026-07-27, 2026-08-02` ✓ |
  | manifest หมดแล้ว | `manifest ends 2026-08-02 -- produce the next batch` ✓ |
  | manifest ว่าง | `Manifest has no dated clips -- produce the next batch` ✓ |
- **ผลพลอยได้:** ตอนนี้ guard จะเตือนเองเมื่อคลังคลิปใกล้หมด — เดิมต้องรอคนสังเกต

### 3. เอกสาร/commit ลงวันที่ 25 ก.ค. แต่งานส่วนท้ายทำหลังเที่ยงคืน
- ไม่กระทบระบบ แต่บันทึกไว้เพื่อให้ timeline อ่านย้อนได้ถูก: commit 4 ตัวแรก = 25 ก.ค. 21:52–22:18 · commit ที่ 5 = 26 ก.ค. 01:37 · ไฟล์นี้ = 26 ก.ค. 02:00

---

## ✅ ตรวจซ้ำแล้วถูกต้อง (หลักฐานจากการรันจริง ไม่ใช่การอ่านโค้ด)

| # | สิ่งที่ตรวจ | วิธี | ผล |
|---|---|---|---|
| 1 | 12 หน้า intent guard ไม่มี affiliate above-fold | เทียบตำแหน่ง CTA กับ `<h1>` ทุกหน้า | **PASS 12/12** |
| 2 | CTA LINE deep-link | grep URL-encoded | **13/13 หน้า** |
| 3 | ลิงก์ `ti/p` เดิมไม่ถูกแตะ | นับทั้ง site | **72 จุด ครบ** |
| 4 | build gate | `postdeploy_smoke.py --src site` | **71/71 · atth.me 185 · PASS** |
| 5 | disclosure ถูกต้องหลังแก้ | ทุกหน้าใน 12 ยังมี affiliate ≥2 ในเนื้อหา → ต้องใช้ "* มีลิงก์พันธมิตร" (ไม่ใช่ประโยคปฏิเสธ) | **ถูกทุกหน้า** (`ไม่มีลิงก์พันธมิตร` = 0) |
| 6 | guard ไม่ขอ token อีก | รันจริง 26 ก.ค. / 27 ก.ค. / 5 ส.ค. / 20 ส.ค. | **ไม่มีคำขอ credential เหลือ** |
| 7 | IG PAUSED ทำงานตามช่วง | รัน `--date` หลายวัน | `PAUSED ... until 2026-08-25` ✓ |
| 8 | manifest + ไฟล์ batch3 | อ่าน manifest + `ls reels/` | **ครบ 27 ก.ค.–2 ส.ค. · captions ครบ 5 ช่องทุกวัน** |
| 9 | syntax หลังแก้ | `py_compile` | **OK** |

### ตรวจลึกพิเศษ — affiliate ที่ "เหลือไว้" ในหน้าวิกฤตเหมาะสมไหม
เอาออกเฉพาะ above-fold แล้ว ในเนื้อหายังมี 2–5 ตัว จึงไล่อ่านบริบทก่อนปุ่มทุกอัน:

| หน้า | ข้อความปุ่ม | ตัดสิน |
|---|---|---|
| คลินิกแก้หนี้ SAM | "หนี้เกินเพดาน **หรือยังไม่เป็นหนี้เสีย?** ดูสินเชื่อรวมหนี้…" | เหมาะสม — พูดถึงกรณีที่คลินิกรับไม่ได้ ไม่ใช่ดักคนที่คลินิกช่วยได้ |
| หนี้บัตรโดนฟ้อง | "**ก่อนถึงชั้นศาล** รีบจัดการ…" | เหมาะสม — เป็นทางเลือกก่อนถึงจุดที่แก้ยาก |
| โดนอายัดเงินเดือน | "ทางที่ดีที่สุดคือจัดการหนี้ **ก่อนถึงชั้นบังคับคดี**…" | เหมาะสม |

ทุกปุ่มมี `* มีลิงก์พันธมิตร` นำหน้า → **ไม่ต้องแก้เพิ่ม** การเอาออกเฉพาะ above-fold คือขอบเขตที่ถูกแล้ว

---

## ⚠️ ยืนยันไม่ได้ตอนนี้ (รายงานตรง ไม่เดา)
**LINE OA auto-reply `เคส-triage`** — เปิดหน้า manager.line.biz แล้วเจอ login wall (session หมดอายุ) → หยุดตามกฎ ห้ามกรอกรหัส
หลักฐานที่มี: ยืนยันจากเซิร์ฟเวอร์เมื่อ 25 ก.ค. ~21:45 — ตาราง auto-response แสดง `เคส-triage | ตอบกลับตามคีย์เวิร์ด (3) | ตลอดเวลา` และหัวหน้าเพจแสดง `แชท: เปิด` + `วิธีตอบข้อความ: แชทแบบแมนนวล + ข้อความตอบกลับอัตโนมัติ`
**สิ่งที่ควรทำ:** เจ้าของเปิด LINE แล้วพิมพ์คำว่า `เคส` หาบัญชีตัวเอง 1 ครั้ง — ได้ทั้งยืนยัน auto-reply และทดสอบ deep-link บนมือถือในคราวเดียว

---

## 📌 บทเรียนรอบนี้ (ต่อจากบทเรียนรอบก่อน)
รอบก่อนได้ว่า *"task พังเพราะรันสำเร็จโดยใช้แผนที่เลิกใช้แล้ว"*
รอบนี้ได้เพิ่มอีกชั้น: **โค้ดที่เขียนสถานะถาวรเป็น "ชุดวันที่ชั่วคราว" จะตายเงียบโดยไม่มีใครรู้** — ไม่ error ไม่ fail แค่เปลี่ยนไปตอบอีกแบบเงียบๆ

กฎที่ควรถือต่อไป: **นโยบายถาวร → เขียนเป็น `FROM date` เปิดปลาย · เหตุการณ์ชั่วคราวจริง → เขียนเป็นชุดวันที่ พร้อมคอมเมนต์ระบุว่าหมดอายุเมื่อไหร่และต้องทำอะไรต่อ**
ชุดวันที่ที่เหลืออยู่ในไฟล์ตอนนี้ (`UI_SCHEDULED_IG_DATES` 13–19 ก.ค. · `TIKTOK_UI_SCHEDULED_DATES` 13–26 ก.ค.) ตรวจแล้ว = เป็นเหตุการณ์ชั่วคราวจริง (batch2 ตั้งเวลา UI) และหมดอายุแล้วอย่างถูกต้อง — batch3 เป็น manual upload จึงต้องให้ guard เช็คโปรไฟล์จริง ✓

---
*ตรวจ 9 ด้าน · พบเพิ่ม 3 · แก้ครบ · ทุกข้อยืนยันด้วยการรันจริง ยกเว้น LINE OA ที่ session หมดอายุ*
