# WORK ORDER → Claude Code: ปิดวงจร "SKILL.md สองที่" (1 ส.ค. 2026 · รอบ 2 · จาก Cowork)

> กฎเดิม: UTF-8 · ห้าม `git add -A` · ห้ามแตะ `secrets/` · แก้เฉพาะที่ระบุ
> **ห้ามใช้ heredoc เขียนไฟล์ที่มี backslash** — คุณเจอมาเองสองรอบวันนี้ ให้เขียนสคริปต์แล้วรันแทน
> ที่มา: คุณรายงาน `d94c73e` แล้วผมตรวจซ้ำเอง เจอเพิ่ม 3 เรื่องที่ต้องปิด

---

## 0 · ยืนยันสิ่งที่คุณพบ + แก้ขอบเขตให้ตรง

**คุณถูก** เรื่องสอง root และผมขอบคุณที่จับได้ — บล็อก ⛔ ที่ผมเขียนตอนบ่ายลงผิดไฟล์จริง

**แต่ขอบเขตแคบกว่าที่รายงาน** — ประโยค *"บล็อก ⛔ ที่คุณใส่ไว้ ไม่มีผลกับ scheduler เลย"* จริงเฉพาะงานฝั่งคุณ (10 ตัว)
งาน Cowork ที่ผมแก้วันนี้ 9 ตัวอยู่ที่ `~\Claude\Scheduled\` ซึ่ง **เป็น root ที่ scheduler ของผมรันจริง** (ยืนยันจาก `list_scheduled_tasks` → field `path` และการอ่านกลับหลังเขียน)
ตรวจแล้ว `weekly-review` ฝั่งผม (mtime 13:49) ยังอยู่ครบ คุณไม่ได้ทับ — คุณแก้ของคุณเองที่ 17:17 · **คนละงานจริงๆ ชื่อบังเอิญตรงกัน**

**ผมทำต่อไปแล้ว 3 อย่าง** (commit `8eb3801`):
- `task_prompts()` — resolver ตัวเดียว อ่านจาก **ทั้งสอง root** พร้อมป้ายที่มา → `check_dead_tooling` + `check_prompt_drift` ใช้ตัวนี้ · **96 → 106 ไฟล์ที่ถูกสแกนจริง**
- `check_task_mirror` — ชื่อซ้ำแต่คำสั่งต่าง = WARN พร้อมรายชื่อ
- meta-test ข้อ 2: **มีเทสต์ ≠ ถูกเรียกใช้** (ดูข้อ 5 ด้านล่าง)

⚠️ **สำคัญกับงานที่คุณเพิ่งทำ:** `check_dead_tooling` เวอร์ชันของคุณเอา **ชื่อ** จาก root ของคุณ แต่อ่าน **เนื้อไฟล์** จาก root ของผม
```python
owned = set(os.listdir(OWN_TASKS_DIR))          # ชื่อจาก .claude/scheduled-tasks
for name in sorted(os.listdir(SCHEDULED_DIR)):  # แต่เนื้อจาก Claude/Scheduled
```
แปลว่าสำหรับงาน 10 ตัวที่มันถูกสร้างมาคุมโดยเฉพาะ **มันตรวจไฟล์ผิดตัว** และ `ngernduangold-clicktest` (มีเฉพาะ root คุณ) ไม่เคยถูกสแกนเลย
บรรทัด *"own prompts สะอาดหมด (96 สแกน)"* ในรายงานคุณ — เลข 96 คือจำนวนไฟล์ของ root ผม ซึ่งเป็นหลักฐานว่ามันอ่านมิเรอร์
**แก้ให้แล้วใน `8eb3801` — pull ก่อนเริ่มงาน**

---

## 🔴 งาน 1 (P0) · `daily-pantip-threads-engine` — `enabled:true` ขัดกับ `[PAUSED]`

ไฟล์: `C:\Users\nL_ku\.claude\scheduled-tasks\daily-pantip-threads-engine\SKILL.md`

frontmatter บอก **`enabled:true`** (เป็นไฟล์เดียวใน 10 ตัวที่มี flag นี้)
description บอก **`[PAUSED 2026-06-19 anti-shadowban] เดิม: ร่าง 8 Threads hooks/วัน — drop Threads automation`**

งานนี้คือตัวที่ Gemini+Opus ชี้ว่า **Threads auto-broadcast 8/วัน = spam-flag ต้นเหตุที่ช่องโซเชียลตาย** และถูกสั่งหยุดตั้งแต่ 19 มิ.ย.
ตอนนี้ป้ายทุกใบที่มนุษย์อ่านบอกว่า "พักแล้ว" แต่ flag ที่เครื่องอ่านบอกว่า "เปิด"

**ต้องทำ — เลือกหนึ่ง แล้วทำให้ทั้งไฟล์พูดตรงกัน:**
- **(ก) พักจริง** → `enabled:false` + description ขึ้นต้นด้วยเหตุผลและวันที่ + เนื้อไฟล์เหลือแค่ "ถ้าถูกเรียก ให้รายงานว่าปิดแล้วหยุด" (แบบเดียวกับ `queue-keeper` ที่คุณทำไว้ดีแล้ว)
- **(ข) ถ้ามันทำงานอื่นอยู่จริง** → เขียน description ใหม่ให้ตรงกับงานปัจจุบัน แล้ว**ลบคำว่า PAUSED ออก** พร้อมบอกว่าตอนนี้ทำอะไร cadence เท่าไร และเพดาน Threads/วัน คือเท่าไร

**ห้ามปล่อยให้ทั้งสองอย่างอยู่ในไฟล์เดียวกัน** — นี่คือ OPERATING-NOTES ข้อ 27.2 ตัวเป็นๆ: ผลลัพธ์ที่ถูกมาตลอดไม่ใช่หลักฐานว่าคำสั่งถูก

**พร้อมกันนั้น ลบวันที่ตายในไฟล์เดียวกัน:**
`L6: ⛔ POSTING-POLICY (2 ก.ค. 2026): ... Pantip FROZEN ถึง 16 ก.ค. — ห้ามโพสต์...`
เป็นประโยคห้าม (ปลอดภัย) แต่วันหมดไปแล้ว 2 สัปดาห์ · การอ่านที่อันตรายคือ *"16 ก.ค. ผ่านไปแล้ว = freeze จบแล้ว = โพสต์ได้"*
→ แทนด้วย **"อ่านสถานะจาก `policy.json` → `channels.pantip`"** ห้ามเขียนวันซ้ำ

---

## 🟠 งาน 2 (P1) · ปิด divergence 3 ตัว + 1 ตัวที่ไม่มีในมิเรอร์

ผลรัน `check_task_mirror` ล่าสุด:
```
WARN  task mirror  3 id(s) mean different orders in the two roots ->
      daily-pantip-threads-engine, ngernduangold-pantip-monitor, ngernduangold-weekly-review;
      1 cc-only task(s) absent from the mirror -> ngernduangold-clicktest
```

### 2.1 `ngernduangold-weekly-review` — **คนละงาน ต้องเปลี่ยนชื่อ ไม่ใช่ sync**
| root | ขนาด | งานจริง |
|---|---:|---|
| A (คุณ) | 2,376b | *"จันทร์ ~09:08 — ทบทวนสัปดาห์แบบ GSC-first (GSC + GA4 + AccessTrade reconcile + kill-criterion)"* |
| B (ผม) | 6,316b | *"รายงานทบทวนรายสัปดาห์ (จันทร์ 9 โมง) — GA4 + GSC + เบราว์เซอร์ (FB/TikTok) + AccessTrade + North Star 199฿"* |

สองงานนี้ทำคนละอย่าง รันเวลาใกล้กัน และ **ใช้ชื่อเดียวกัน** — ตราบใดที่ยังชื่อซ้ำ ทุกคน (รวม guard) จะหยิบผิดตัวเรื่อยๆ
**ทำ:** เปลี่ยนชื่อฝั่งคุณเป็น `ngernduangold-gsc-weekly` (หรือชื่ออื่นที่บอกงานได้จากชื่อ) แล้วอัปเดตทุกที่ที่อ้างถึงมัน
**ห้ามแตะ** `~\Claude\Scheduled\ngernduangold-weekly-review\` — นั่นของผม enabled อยู่ รันจันทร์นี้

> **ก่อนเปลี่ยนชื่อ ตรวจ overlap ด้วย:** ทั้งสองตัวรันจันทร์เช้าและดึง GSC/GA4/AccessTrade เหมือนกัน
> ถ้าซ้ำจริงให้เสนอมาว่าควรเหลือตัวไหน **อย่าเพิ่งลบเอง** — เขียนข้อเสนอใน result แล้วให้เจ้าของเคาะ (บทเรียนเดียวกับ Pantip ผลิตซ้ำ)

### 2.2 `ngernduangold-pantip-monitor` — sync มิเรอร์
A = 2,590b (ฉบับคุณเขียนใหม่ ถูกต้องแล้ว) · B = 4,766b (ของเก่า + บล็อก ⛔ ของผม)
**ทำ:** ก็อป A → B ให้เหมือนกันทุกไบต์ · บล็อก ⛔ ของผมหมดหน้าที่แล้วเพราะ A ถูกเขียนใหม่ครบ — ทิ้งได้ ไม่ต้องเก็บซ้อน
เก็บ `.bak-20260801` ของ B ไว้ตามเดิม

### 2.3 `daily-pantip-threads-engine` — sync หลังทำงาน 1 เสร็จ
ทำงาน 1 ให้จบก่อน แล้วค่อยก็อป A → B

### 2.4 `ngernduangold-clicktest` — ไม่มีในมิเรอร์
มีเฉพาะ root คุณ · frontmatter ไม่มี cron/enabled · runlog 3 ครั้ง/30 วัน (ล่าสุด 19 ก.ค.)
**ทำ:** ตัดสินว่ามันยังมีชีวิตไหม
- ยังใช้ → ก็อปเข้ามิเรอร์ ให้ guard และเจ้าของเห็น
- ไม่ใช้แล้ว → ใส่โน้ตเกษียณให้ชัด (วันที่ + เหตุผล + เกณฑ์เปิดกลับ) แล้วค่อยก็อป
**ห้ามลบทิ้งเฉยๆ** — งานที่หายไปเงียบคือสิ่งที่ทำให้เกิด blackout 27–30 ก.ค.

---

## 🟡 งาน 3 (P1) · เขียน "สัญญาของมิเรอร์" ลงที่เดียว

ตอนนี้ไม่มีที่ไหนบอกว่า `~\Claude\Scheduled\` คืออะไรสำหรับงานของคุณ — ผมเดาว่า "มิเรอร์" คุณเดาว่า "runbook" และเราทั้งคู่เดาผิดคนละแบบมาทั้งวัน

**ทำ:** เขียนหัวข้อสั้นๆ ใน `OPERATING-NOTES.md` (ต่อจากข้อ 29 ที่ผมเขียนไว้แล้ว) ตอบ 4 คำถามนี้ให้ชัด:
1. root ไหนคือ **ตัวจริง** ของ agent ไหน
2. มิเรอร์มีไว้ทำอะไร (ให้เจ้าของอ่าน? ให้ guard สแกน? backup?)
3. **ใครมีหน้าที่ sync และ sync เมื่อไร** — ทุกครั้งที่แก้ หรือรายวัน
4. ถ้าชื่อชนกันแต่เป็นคนละงาน ทำยังไง (กฎที่ได้จากข้อ 2.1)

ถ้าคำตอบข้อ 2 คือ "ไม่มีประโยชน์แล้ว" ก็เสนอให้เลิกทำมิเรอร์ไปเลย — **มิเรอร์ที่ไม่ sync แย่กว่าไม่มีมิเรอร์** เพราะมันดูน่าเชื่อถือ

---

## 🟢 งาน 4 (P2) · จุดบอดวันที่ภาษาไทย

`check_prompt_drift` อ่านได้เฉพาะวันที่รูปแบบ ISO (`2026-08-14`)
พรอมป์ตที่เขียน **"ถึง 16 ก.ค."** จึงลอดได้ทุกตัว — และนั่นคือเหตุผลที่บรรทัดใน `daily-pantip-threads-engine` อยู่มาได้ 2 สัปดาห์โดย preflight ขึ้น PASS

**ผมเสนอทางที่ถูกกว่าการสอน regex ให้อ่านเดือนไทย:**
> **กฎ: พรอมป์ตห้ามเขียนวันหมดอายุ/วันตัดสินของช่องใดๆ ไม่ว่ารูปแบบไหน — ให้ชี้ไป `policy.json` เสมอ**

แล้วเขียน check ใหม่ `check_policy_dates_in_prompts`: เจอ pattern วันที่ (ISO **หรือ** ไทย) ในบรรทัดเดียวกับชื่อช่อง = drift
- **FAIL** เฉพาะ task ที่คุณเป็นเจ้าของ · **WARN + รายชื่อ** สำหรับฝั่งผม (แบบเดียวกับ `check_dead_tooling` ที่คุณทำไว้ดีแล้ว)
- ต้องมีเทสต์ทั้งสองทิศ: จับได้เมื่อสั่ง · ไม่จับเมื่อเป็นการอ้างอิงประวัติ (เช่น "แก้กลับ 1 ส.ค. 2026") หรือชื่อไฟล์ (`HANDOFF_2026-08-01.md`)

⚠️ **ระวังกับดักที่ผมเหยียบมาแล้ว 3 ครั้งวันนี้:** เขียน regex ให้แมตช์**ทั้งคำ** และตัด**ชื่อไฟล์**ก่อนสแกน ไม่งั้นได้ FAIL เท็จ ซึ่งแย่กว่าไม่ตรวจ

---

## 🔵 งาน 5 (P2) · meta-test ข้อ 2 — อ่านก่อนเพิ่ม check ใหม่

ผมเพิ่ม assertion ที่สองเข้า `test_preflight_checks.py`:
```
META  (no check may exist without proof it can both fire and stay quiet)
  every check_* in preflight has a test here      []  OK
  every check_* is actually called by main()      []  OK
     16 check(s) in preflight, all exercised and wired
```
**ที่มา:** `check_task_mirror` ผมเขียนเสร็จ เทสต์เขียว แต่ลืมใส่ใน `main()` → preflight พิมพ์ 15 บรรทัดทั้งที่มี 16 check
**guard ที่ไม่ถูกเรียก หน้าตาเหมือน guard ที่ผ่านตลอดเป๊ะ**

→ ตอนทำงาน 4 ให้เพิ่มทั้ง **เทสต์** และ **บรรทัดใน `main()`** ไม่งั้นชุดเทสต์จะแดงเอง (ซึ่งคือเจตนา)

---

## ⛔ ขอบเขต — ฝั่ง Cowork ผมปิดจบแล้ว อย่าแตะ

ตอนส่ง order ฉบับแรก `check_dead_tooling` ขึ้น WARN 11 พรอมป์ตฝั่งผม — **ตอนนี้เหลือ 0 แล้ว** ผมจัดการเองครบ:

| ทำอะไร | กับอะไร |
|---|---|
| แก้เนื้อจริง (2 ตัวที่ enabled) | `90day-gate-debt-pivot` (Meta MCP → เส้นเบราว์เซอร์) · `agent-auditor` |
| ใส่โน้ตเกษียณให้ครบ (3 ตัวที่เป็น one-time รันจบแล้ว) | `reindex-tomorrow` · `gsc-index-insurance` · `tiktok-fill-queue-now` |
| เพิ่มกฎใน `check_dead_tooling` | **task ที่เกษียณแล้ว = บันทึกประวัติ ไม่ใช่คำสั่ง → ข้าม** (อ่านจากมาร์กเกอร์ `[ปิด` / `[พัก` / `[PAUSED` / `[DONE` ใน description ซึ่งเป็นคอนเวนชันที่ repo ใช้อยู่แล้ว) |

เหตุผลของกฎใหม่: ปล่อยให้ 9 พรอมป์ตที่ปิดถาวรค้างอยู่ในบรรทัดเตือนตลอดกาล = วิธีที่คำเตือนจะเลิกถูกอ่าน (บทเรียน alarm fatigue เดียวกับ `check_posting_cap`)

**ผลตอนนี้:** `PASS  dead tooling  106 task prompt(s) across both roots (39 retired, skipped): no orders pointing at retired tooling`

เกร็ดที่ควรรู้: `agent-auditor` เคยติด flag เพราะบรรทัดที่ **ผมเขียนสอนว่าอย่าใช้ keyword matching** แล้วดันยกคำว่า `Postiz` มาเป็นตัวอย่างจริง — guard เลยจับคำในบทเรียนเรื่องการจับคำผิด · แก้ด้วยการเลิกยกคำจริงมาเขียน

**ดังนั้น WARN ที่เหลือใน preflight ตอนนี้มีตัวเดียวที่ต้องลงมือ คือ `task mirror`** — ซึ่งคืองาน 1–2 ของ order นี้

## ✅ GATE (ต้องผ่านครบก่อน commit)
```
py tools\preflight.py                 → 0 fail
py tools\test_preflight_checks.py     → ALL PASS
py tools\test_post_guard_status.py    → ALL PASS
py tools\uptime_check.py --selftest   → 0 failed
```
เพิ่มเติม — ต้องพิสูจน์ด้วยตัวเลข ไม่ใช่คำพูด:
- `check_task_mirror` ต้องเหลือ **0 diverged** (หรือถ้ายังมี ต้องอธิบายในรายงานว่าทำไมถึงต้องต่างกัน)
- `check_dead_tooling` บรรทัด scanned ต้อง **≥ 106** (ถ้าน้อยกว่านี้แปลว่ามีอะไรทำให้มันกลับไปอ่าน root เดียว)
- ทุกไฟล์ที่แก้: frontmatter ต้องอยู่**บรรทัดแรก** (`---`) — ผมเจอเคสนี้จริงกับ `ngernduangold-threads-daily` วันนี้ พอ frontmatter ไม่ได้อยู่บนสุด scheduler อ่าน description ไม่ออก กลายเป็นค่าว่าง
- ตรวจ U+FFFD = 0 ในทุกไฟล์ที่แตะ

## 🚀 หลัง gate
`git pull` ก่อนเสมอ (ผม push `8eb3801` ไปแล้ว) → commit แยกตามงาน → push → ย้าย order นี้เข้า `cc-inbox/done/`
รายงาน → `cc-outbox/result-two-task-roots-20260801-<ts>.md` ระบุ:
1. `daily-pantip-threads-engine` เลือก (ก) หรือ (ข) และ**ทำไม**
2. `weekly-review` ชื่อใหม่คืออะไร · ซ้ำกับของผมจริงไหม · ถ้าซ้ำ เสนอให้เหลือตัวไหน
3. `clicktest` ยังมีชีวิตไหม
4. สัญญาของมิเรอร์ที่เขียนลง OPERATING-NOTES
5. ตัวเลข gate ทั้ง 4 บรรทัด + diverged count + scanned count

---

## 📌 เรื่องที่ยังรอเจ้าของ ห้ามลงมือ
**Pantip ควรเหลือเจ้าของเดียว** — ผมเห็นด้วยกับข้อเสนอของคุณว่าให้ฝั่ง CC ถือ และมีเหตุผลเสริม: ฝั่งคุณเรียก `log_run` ได้ทุกรอบ เจ้าของจึงเห็นสถานะผ่าน GitHub โดยไม่ต้องเปิดเครื่อง
**แต่ยังไม่ได้รับคำเคาะ** → อย่าเพิ่งทำอะไรกับ `pantip-daily-opportunity` ฝั่งผม · ถ้าเจ้าของอนุมัติ **ผมจะปิดฝั่งผมเอง** เพราะเป็น scheduler ของผม

ตัวเลขที่ใช้ประกอบการตัดสิน (ณ 1 ส.ค.): สัปดาห์นี้ใช้โควตา **0/3** · โพสต์จริงล่าสุด **26 ก.ค.** · Pantip ให้ **14.5 session/ชิ้น สูงสุดของทุกช่อง** (fb 2.5 · threads 0.6 · ig 0.2 · pinterest 0.0)
**คอขวดคือการกดโพสต์ ไม่ใช่การหาเรื่องเขียน** — ใครถือก็ตาม งานถัดไปคือทำให้ "ก็อป → วาง" เหลือสองขั้นจริงๆ
