# CC ORDER — ทำให้ gate รู้จัก "ช่วงที่ระบบเงียบ" + จัดการคิวหลัง 8 ส.ค.
**ออกคำสั่ง:** Cowork · 7 ส.ค. 2026 · **P0 — ข้อ 1 ต้องเสร็จก่อน 8 ส.ค. 09:30**

---

## 0 · สิ่งที่ Cowork ทำไปแล้ววันนี้ (อย่าทำซ้ำ อย่าย้อน)

| ทำอะไร | ผล |
|---|---|
| `tools/agent_gap_check.py` ใหม่ + ต่อเข้า `run_daily.cmd` | ชั้น cron จับได้แล้วว่าชั้น agent เงียบ (>26 ชม. เขียนไฟล์เตือน) · 16 เทสต์ |
| ย้าย `b3-07` → **7 ส.ค.** · `b3-04` → **8 ส.ค.** ครบทั้ง 3 ไฟล์ | runway 0 → **2 วัน** · `queued clip spec` PASS (1080×1920 ครบ) |
| GA4 เพิ่มไอพี `27.130.8.134/32` | `ga4 internal ip` กลับเป็น PASS |
| เปิด Gumroad ยืนยันด้วยตา | **ขายได้จริง** — ปุ่ม "I want this!" + ไฟล์ 22.7 KB แนบครบ |
| `policy.json` | เพิ่ม `blackouts[]` · `funnel_endpoint.gumroad` · `ga4.internal_traffic.rotation_history` |

`preflight` = **0 fail / 7 warn** · เทสต์ **16 + 154 + 23 ผ่านหมด** · commit อยู่บน `main` แล้ว

---

## 1 · 🔴 `ngernduangold-buy-intent-gate` ยิงพรุ่งนี้ — **ห้ามให้มันตัดสิน** (ก่อน 8 ส.ค. 09:30)

### ปัญหา
gate ตั้งให้นับจาก 2 ส.ค. แต่ **3, 4, 5 ส.ค. ชั้น agent ตายสนิท** — ไม่มีโพสต์ ไม่มีทราฟฟิกถูกดัน ไม่มีอะไรถูกวัด
หน้าต่างที่สะอาดจริงคือ **6–7 ส.ค. = 2 วัน ไม่ใช่ 7 วัน**

ถ้าปล่อยให้รันตามปกติ มันจะอ่าน `buy_intent_click` ได้น้อย แล้วสรุปว่า **"ไม่ผ่าน"**
ทั้งที่**การทดสอบไม่เคยถูกรัน** — นี่คือสิ่งที่เพิ่งเกิดกับ `batch4-gate` เมื่อ 6 ส.ค. เป๊ะ ๆ

### ให้ทำ (เลือก ก. ถ้าทำได้)
**ก. เลื่อน gate ออกไปเท่าจำนวนวันที่เงียบ** → `fireAt` ใหม่ = **11 ส.ค. 09:30** (8 + 3 วัน)
แล้วแก้ `policy.json → gates[buy-intent-gate].date` ให้ตรงกัน **ทั้งสองที่ต้องตรงกันเสมอ**

**ข. ถ้าเลื่อนไม่ได้** ให้แก้ prompt ของ gate ให้**ขึ้นต้นรายงานด้วยบรรทัดนี้ก่อนตัวเลขใด ๆ**:
> ⚠️ หน้าต่างวัดผลสะอาดจริง N วัน (6–7 ส.ค.) เพราะ 3–5 ส.ค. เป็น blackout — **ตัวเลขข้างล่างยังตัดสินไม่ได้**

และ**ห้ามออกคำว่า "ผ่าน/ไม่ผ่าน"** ให้ออกได้แค่ `ยังตัดสินไม่ได้`

### ห้ามเด็ดขาด
- ห้ามเทียบกับตัวเลขก่อน 2 ส.ค. — ช่วงนั้น `affiliate_click` มีคลิกของ automation เราเองปนอยู่ (ดู `ga4.clicktest_isolation`)
- ห้ามลบข้อมูลย้อนหลังใน GA4

---

## 2 · 🔴 คิวหมดอีกครั้ง **9 ส.ค.** — ต้องตัดสิน batch4 ภายใน 8 ส.ค.

ตอนนี้มีของถึง 8 ส.ค. เท่านั้น (b3-07 พรุ่งนี้ · b3-04 มะรืน) **9 ส.ค. เป็นต้นไปว่างเปล่า**

`GATE-batch3_20260806.md` สรุปว่า **"ยังตัดสินไม่ได้"** เพราะการทดสอบไม่เคยถูกรันจริง
(YouTube 5/7 · Facebook 2/7 · Threads 3/7 · TikTok 0/7 · 3 ใน 7 วันว่างเปล่า)

**ข้อเสนอของ Cowork — CC ตัดสินและบอกเหตุผลกลับมา:**
> **อย่าเพิ่งสร้าง batch4 เต็มชุด** ให้ทำ **batch3-rerun**: เอา 7 คลิปเดิมที่มีอยู่แล้วมาลงให้ครบทุกช่องตามตารางจริง
> เหตุผล: เราไม่เคยรู้ว่า batch3 ดีหรือไม่ดี เพราะมันไม่เคยถูกส่งครบ — ผลิตชุดใหม่ตอนนี้คือ**จ่ายซ้ำเพื่อไม่ได้คำตอบอีกรอบ**
> คลิปที่มีอยู่และยังไม่ถูกใช้ครบทุกช่อง: `b3-02` `b3-03` `b3-06` (FB 0 ช่อง) · `b3-07` `b3-04` (ยังไม่เคยลงที่ไหน)

ถ้า CC เห็นต่าง ให้บอกมาพร้อมตัวเลข **อย่าเงียบแล้วผลิตต่อ**

---

## 3 · 🟡 ยืนยันว่าการย้ายวันของ Cowork เข้ากับ routine ของ CC จริง

ผมย้าย `2026-08-04 → 2026-08-07` และ `2026-08-05 → 2026-08-08` ใน **3 ไฟล์**:
`.system_control/content_manifest.json` · `reels/schedule.json` · `social-autopost/content_map.json`

ตรวจก่อนย้ายแล้วว่าทั้งสองชิ้น `posted` เป็น `null` ทุกช่อง (ไม่มีประวัติให้พัง) และ `runway_guard` ตอนนี้เห็น 2 วันตรงกันทั้งสามแหล่ง

**ให้ CC ตรวจ:** routine ฝั่ง CC (โดยเฉพาะตัวตั้งเวลา YouTube) หยิบสองวันนี้ขึ้นจริงไหม
ถ้ามีไฟล์ที่ 4 ที่ผมไม่รู้จักและยังค้างวันเดิมอยู่ **บอกมาทันที** — นั่นคือ bug คลาส "ข้อเท็จจริงเดียวเก็บหลายที่" ที่ทำให้ blackout 27–30 ก.ค.

---

## 4 · 🟢 งานเชิงระบบ — ทำให้ gate ทุกตัวรู้จัก blackout เอง

ผมเพิ่ม `policy.json → blackouts[]` ไว้แล้ว บันทึกทั้งสองครั้ง (27–30 ก.ค. · 3–5 ส.ค.) พร้อมสาเหตุและราคาที่จ่าย

**ให้ CC เพิ่มด่านเดียวกันนี้ในทุก gate:**
```
ก่อนคำนวณอะไรก็ตาม:
  1. อ่าน policy.json -> blackouts[]
  2. ถ้าหน้าต่างวัดผลของ gate ทับกับ blackout ใด ๆ
     -> ห้ามออกคำตัดสิน ให้ออก "ยังตัดสินไม่ได้ (หน้าต่างสกปรก N วัน)"
     -> เสนอวันใหม่ = วันเดิม + จำนวนวันที่ทับ
```
**เหตุผลที่ต้องเป็นด่านอัตโนมัติ ไม่ใช่ให้คนจำ:** วันนี้เราจับได้เพราะบังเอิญมีคนไปตรวจ Slack
gate ตัวถัดไปจะเจอเรื่องเดียวกัน และตอนนั้นอาจไม่มีใครตรวจ

---

## 5 · 🟡 TikTok — 0/7 ตลอด batch3 ยังไม่มีใครตัดสิน

TikTok ไม่ได้รับคลิปเลยสักชิ้นใน batch3 และ reach ยุบตั้งแต่ 20 ก.ค. (คลิปล่าสุด 0–1 วิว ผู้ติดตาม 13)
`batch4-gate` ควรตัดสินเรื่องนี้แต่ตัดสินไม่ได้

**ให้ CC ระบุกลับมาเป็นข้อความ:** ต้องเห็นหลักฐานอะไรถึงจะบอกได้ว่า "ช่องนี้ตายแล้ว" — และเราเก็บหลักฐานนั้นได้จริงไหม
ถ้าเก็บไม่ได้ (เช่นต้องเปิดแอปมือถือดูสถานะบัญชี ซึ่งมีแต่เจ้าของทำได้) **ให้บอกตรง ๆ ว่าเป็นงานของเจ้าของ** อย่าปล่อยให้ค้างในสถานะ "รอข้อมูล" ต่อไปอีก 3 สัปดาห์

---

## 🚫 ข้อห้าม
- ห้ามแตะ `.system_control/host_ip.json` — เขียนโดย `uptime_check.py` เท่านั้น แก้มือ = ทำให้ guard ตาบอด
- ห้ามแก้กฎ internal traffic ใน GA4 เอง (Cowork ถือ UI ฝั่งนั้น) — ถ้าไอพีหมุนอีก ให้บอกมา
- ห้ามขยาย CIDR เป็น `/16` เพื่อกันไอพีหมุน — `27.130.0.0/16` เป็นช่วง ISP ผู้ใช้ทั่วไปในไทย มีคนอ่านจริงอยู่แน่นอน
- ห้าม `enabled:false` งานที่ยังทำงานอยู่โดยไม่ใส่โน้ตเกษียณ
- ห้าม token/key ในไฟล์หรือใน chat

## ✅ นิยาม "เสร็จ"
1. `buy-intent-gate` ไม่ออกคำว่า ผ่าน/ไม่ผ่าน ในหน้าต่างที่สกปรก — พิสูจน์ด้วยการรัน dry-run ให้ดู
2. มีแผนคอนเทนต์ครอบคลุม **9 ส.ค. เป็นต้นไป** พร้อมเหตุผล
3. ตอบข้อ 3 (มีไฟล์ที่ 4 ไหม) และข้อ 5 (เกณฑ์ตัดสิน TikTok) กลับมาเป็นข้อความ
4. `py tools\preflight.py` ไม่มี FAIL ใหม่ · `py tools\test_preflight_checks.py` + `py tools\agent_gap_check.py --selftest` ยังผ่านครบ

---
---

# 📌 แก้ไขเพิ่มเติม — 7 ส.ค. 01:1x (เจ้าของสั่ง "คุมลุยแทน")
**ข้อ 2 และ ข้อ 5 ถูกตัดสินแล้วโดย Cowork — ไม่ต้องตัดสินซ้ำ ให้ลงมือตามนี้**

## ข้อ 2 (แก้) · batch4 = **ไม่ผลิตชุดใหม่** ให้ backfill Facebook แทน

**ตัวเลขที่ใช้ตัดสิน** — นับจาก `post-ledger.jsonl` ฟิลด์ `clip_id` (ไม่ใช่จาก manifest ซึ่งค้างอยู่และถูกซ่อมวันเดียวกัน)

| clip | YouTube | Facebook | Threads |
|---|---|---|---|
| b3-01 | ✅ | ✅ | ✅ |
| b3-05 | ✅ | ✅ | ✅ |
| b3-02 | ✅ | ❌ | ✅ |
| b3-06 | ✅ | ❌ | ❌ |
| b3-03 | ✅ | ❌ | ❌ |
| b3-07 | คิว 7 ส.ค. | ❌ | ❌ |
| b3-04 | คิว 8 ส.ค. | ❌ | ❌ |
| **รวม** | **5/7 → 7/7 พรุ่งนี้** | **2/7** | **3/7** |

**เหตุผล:** YouTube เป็นช่องเดียวที่ได้ทดสอบจริง และพรุ่งนี้จะครบ 7/7
Facebook ขาดมากที่สุด **และเป็นช่องที่มีทราฟฟิกพิสูจน์แล้วสูงสุด (33 session เทียบ threads 13)**
ผลิต batch4 ตอนนี้ = **จ่ายรอบสองเพื่อคำตอบที่ยังไม่ได้อยู่ดี**

### ตารางที่ให้ทำ — Facebook backfill (ได้ runway 5 วันไปในตัว)

| วันที่ | คลิป |
|---|---|
| 9 ส.ค. | `b3-02` |
| 10 ส.ค. | `b3-06` |
| 11 ส.ค. | `b3-03` |
| 12 ส.ค. | `b3-07` |
| 13 ส.ค. | `b3-04` |

- **1 ชิ้น/วัน เท่านั้น** — เพดานคือ ≤2/วัน/ช่อง และเว้น ≥3 ชม. แต่ 1/วันปลอดภัยกว่าและเราเพิ่งผิดกฎนี้ไป 31 ก.ค.
- **ห้ามแตะ YouTube** ของคลิปเหล่านี้ — b3-02/06/03 ขึ้น YouTube ไปแล้ว การ re-date ทั้งชิ้นจะทำให้อัปซ้ำ **ให้ backfill เฉพาะช่อง**
- **Threads ไม่ต้องทำอะไร** — `threads-video-idle-window` หยิบชิ้นเก่าสุดที่ยังไม่ขึ้นเองอยู่แล้ว จะไล่เก็บ b3-06 → b3-03 → b3-07 → b3-04 ให้เอง
- ถ้ากลไก backfill เฉพาะช่องยังไม่มี **ให้บอกมาว่าต้องสร้างอะไร** อย่าเงียบแล้วข้ามไป

## ข้อ 5 (แก้) · TikTok = **เลิกใช้** — คำถามถูกตอบแล้ว

**Cowork เปิด TikTok Studio จากเบราว์เซอร์ได้เลย ไม่ต้องใช้มือถือของเจ้าของ** — นี่คือเหตุผลเดียวที่เรื่องนี้ค้างมา 8 วัน

| สิ่งที่เห็นจริง | ค่า |
|---|---|
| โพสต์ที่ยังอยู่ | **37 ชิ้น** · แบบร่าง 0 |
| ความเป็นส่วนตัวของทุกโพสต์ | **"ทุกคน" ทั้งหมด** |
| แบนเนอร์เตือน / จำกัดการมองเห็น | **ไม่มีเลย** |
| ผู้ติดตาม | 12 |
| ยอดชม 7 วันล่าสุด | 9 · โปรไฟล์ 0 · คอมเมนต์ 0 · แชร์ 0 |
| ยอดชมต่อโพสต์ | 1–2 |
| โพสต์ล่าสุด | 26 ก.ค. (batch3 ส่งให้ 0/7) |

⇒ **ไม่ได้โดนแบน ไม่ได้โดน shadowban** — สมมติฐานที่ทำให้เรื่องนี้ค้างไว้**ผิด**
ความจริงคือ 37 โพสต์ซื้อผู้ชมไม่ได้เลย และไม่เคยสร้าง session เข้าเว็บสักครั้ง

`policy.json → channels.tiktok.state = "retired"` พร้อมเงื่อนไขกลับมา (ต้องได้คลิปชุดเดียวกับช่องอื่น ไม่ใช่ตัดเหลือ 10 วิ · ต่อเนื่อง 14 วัน)

## ข้อที่ยังเป็นของ CG เหมือนเดิม
ข้อ **1** (buy-intent-gate พรุ่งนี้) · ข้อ **3** (มีไฟล์ที่ 4 ไหม) · ข้อ **4** (gate อ่าน `blackouts[]`) — **ยังต้องทำ ไม่มีอะไรเปลี่ยน**

> เพิ่มหลักฐานให้ข้อ 3: `manifest.posted` **ค้างจริง** 4 ช่อง (รวมของ 20 ก.ค.) ซ่อมให้แล้วโดยเติมจาก ledger เฉพาะช่องที่ว่าง ไม่ลบของเดิม
> ถ้าปล่อยไว้ routine อาจโพสต์ซ้ำเพราะเห็น `null` — **นี่คือหลักฐานว่าข้อ 3 ไม่ใช่คำถามเชิงทฤษฎี**
