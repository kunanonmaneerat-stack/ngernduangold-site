# RETIRED 2026-08-16: Facebook is manual-only in policy.json.
# Historical publisher scripts remain for audit, but this workflow cannot publish or push.
name: fb-feed-retired
on:
  workflow_dispatch: {}

concurrency:
  group: fb-feed
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired — Facebook publishing is manual-only"
