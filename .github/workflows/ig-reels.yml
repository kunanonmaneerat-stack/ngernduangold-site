# RETIRED 2026-08-16: Meta token automation is disabled by current policy.
name: ig-reels
on:
  workflow_dispatch: {}

concurrency:
  group: ig-reels
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired — Instagram token automation is disabled"
