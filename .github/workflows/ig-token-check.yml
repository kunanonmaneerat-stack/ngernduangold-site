# RETIRED 2026-08-16: do not validate, refresh, or persist Meta tokens.
name: ig-token-check
on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired — token validation and refresh are disabled"
