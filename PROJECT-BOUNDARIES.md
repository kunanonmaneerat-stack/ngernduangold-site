# ขอบเขตโปรเจกต์ — อะไรเป็นของใคร

*เขียน 9 ก.ย. 2026 · เหตุผล: งานจาก 4 โปรเจกต์อยู่ในรายการ scheduled task เดียวกัน
ทำให้ทุกครั้งที่คุยเรื่องเว็บ เรื่องบอทเทรดจะโผล่มาปน และเรื่องที่เร่งที่สุดกลับต้องพึ่ง
"ใครสักคนจำได้แล้วพูดขึ้นมา" ซึ่งไม่ใช่กลไก*

> ไฟล์นี้อยู่ในรีโป ngernduangold เพราะเป็นที่เดียวที่ agent เข้าถึงได้
> **แต่เนื้อหาครอบทุกโปรเจกต์** — อย่าอ่านว่ามันเป็นเอกสารของเว็บอย่างเดียว

> **กฎเดียวของไฟล์นี้:** งานหนึ่งตัวมีเจ้าของโปรเจกต์เดียว
> ถ้าอ่านชื่องานแล้วเดาไม่ออกว่าเป็นของโปรเจกต์ไหน แปลว่าชื่อมันผิด ไม่ใช่คนอ่านผิด

---

## 🤖 A. บอทเทรด cascade-fade — **เงินจริง**

| | |
|---|---|
| โค้ด | `C:\groq_bybit_bot` |
| ธรรมนูญ | `LOOP_POLICY.md` (ในโฟลเดอร์นั้น) — รวมรหัสช่อง Slack |
| Slack | `#bot-audit` · `#bot-decisions` · `#bot-alerts` · `#bot-daily` |
| ความเสี่ยง | **สูงสุดในบรรดาทุกโปรเจกต์** — สั่งซื้อขายด้วยเงินจริง |

**งานที่เป็นของโปรเจกต์นี้**
- `bybit-api-key-rotation` — เตือนหมุน API key
- `cascade-bot-weekly-check` — ตรวจสุขภาพบอทรายสัปดาห์
- `cowork-cc-review-loop` — ลูปตรวจงาน CC ทุก 6 ชม.
  ⚠️ **ชื่อหลอกที่สุดในรายการ** — ขึ้นต้นด้วย `cowork-cc` เหมือนงานเว็บ แต่ทั้งตัวเป็นเรื่องบอทเทรด
- `apex-convexity-validation` — ปิดแล้ว (ถูกแทนด้วย GitHub cron)
- `herald-watchdog-deploy-jun15` — ปิดแล้ว (เสร็จ 15 มิ.ย.)

**ข้อห้ามที่ยกมาจาก LOOP_POLICY — ใช้กับทุก agent**
ห้าม arm/disarm · ห้ามยิงออเดอร์ · ห้ามกด/ปลด PAUSE · ห้ามเปลี่ยน `MICRO_NOTIONAL`
ห้ามรีสตาร์ตบอท · ห้ามแตะ `.env` · ห้ามแตะไฟล์ frozen · ห้ามเปลี่ยนชื่อ/ย้าย/ลบไฟล์ `.db` (กฎ R23)
**ห้ามส่ง API key · `ARM.token` · ยอดเงินจริง ออกไปที่ไหนทั้งสิ้น** (bps / % ส่งได้)

---

## 🏠 B. ส่วนตัว / บ้าน

- `airbnb-tax-pnd90-annual` — ภ.ง.ด.90 (มี.ค.)
- `airbnb-tax-pnd94-midyear` — ภ.ง.ด.94 (ก.ย.)
- `airbnb-monthly-bookkeeping-tm30` — บัญชีรายเดือน + ตม.30
- `verify-full-scan-result` · `full-optimize-and-restart` — ดูแลเครื่อง ปิดแล้วทั้งคู่

ไม่เกี่ยวกับเว็บหรือบอทเลย · ยิงเดือนละครั้งหรือปีละครั้ง · **ไม่ต้องปิดตอนพักโปรเจกต์อื่น**

---

## 📕 C. FeedFlow Studio — ปิดถาวร

- `feedflow-inbox-triage` — ปิด 31 ก.ค. 2026 · ไม่มีสัญญาณ outreach 11 วัน · **ห้ามเปิดใช้**
- `validate-gemini-export-docs` — งานครั้งเดียว เสร็จแล้ว

---

## 🌐 D. ngernduangold (เว็บ + affiliate) — โปรเจกต์หลัก

| | |
|---|---|
| โค้ด | `C:\Users\nL_ku\ngernduangold-site` |
| แหล่งความจริง | `.system_control/policy.json` |
| บทเรียน | `OPERATING-NOTES.md` |
| ผู้ปฏิบัติ | Cowork · CC (Claude Code) · Codex |

**งานที่เหลือทั้งหมดในรายการเป็นของโปรเจกต์นี้** — ไม่ต้องไล่ชื่อ เพราะมันคือส่วนใหญ่
รวมถึงงานที่ชื่อไม่ได้ขึ้นต้นด้วย `ngernduangold-` เช่น
`daily-social-post-reminder` · `cowork-task-watchdog` · `grab-shopee-lazada-links` ·
`check-involve-asia-property` · `threads-post-*`

**สถานะปัจจุบัน:** พักทั้งหมดตั้งแต่ 5 ก.ย. 2026 (`policy.json → blackouts`)
เช็กลิสต์เปิดกลับ: `automation-log/RESUME-CHECKLIST_20260905.md`

---

## ทำไมการแยกนี้สำคัญกว่าที่ดู

**1. เรื่องเร่งด่วนของโปรเจกต์หนึ่ง ไม่ควรพึ่งการสนทนาของอีกโปรเจกต์**
Bybit key หมดอายุ 10 ก.ย. · ตัวเตือนยิงไปแล้ว 29 ส.ค. แล้วปิดตัวเองตามปกติของงานครั้งเดียว
ตั้งแต่นั้นมา สิ่งเดียวที่ทำให้เรื่องนี้ยังถูกพูดถึงคือ **Cowork บังเอิญยกขึ้นมาระหว่างคุยเรื่องเว็บ**
นั่นไม่ใช่กลไก — มันคือความบังเอิญที่จะขาดตอนวันที่ไม่มีใครเปิดคุย

**2. "พักโปรเจกต์" ต้องไม่พักสิ่งที่ไม่ได้อยู่ในโปรเจกต์นั้น**
ตอนพักงานเว็บ 5 ก.ย. ถ้าไม่มีเส้นแบ่ง จะเผลอปิดตัวเตือนภาษีและตัวเฝ้าบอทไปด้วย
ทั้งที่ทั้งสองอย่างไม่เกี่ยวกับเหตุผลที่พัก (ประหยัด token ของงานเว็บ)

**3. ชื่อที่หลอกทำให้ตัดสินใจผิดได้จริง**
`cowork-cc-review-loop` ฟังดูเหมือนงานเว็บทุกประการ แต่มันคือลูปตรวจบอทเทรด
คนที่ปิดมันเพราะคิดว่าเป็นงานเว็บ = ปิดตาที่เฝ้าเงินจริงโดยไม่รู้ตัว

> **กฎ: ถ้าปิดงานเป็นชุด ให้ปิดตาม "โปรเจกต์" ไม่ใช่ตาม "ชื่อที่ดูคุ้น"**
